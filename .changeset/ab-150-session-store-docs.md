---
'@lostgradient/operative': patch
---

Document `textValueStore(new MemoryStorage())` from `@lostgradient/weft/storage` as the copy-paste-runnable in-memory `ConditionalTextValueStore` for `createSessionStore`, matching the pattern operative's own test suite uses.
