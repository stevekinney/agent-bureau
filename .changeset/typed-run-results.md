---
'@lostgradient/operative': minor
---

Rename validated run data from `structuredOutput` to `output`, add cached `unwrap()` and typed output accessors, and expose diagnostic handles for durable runs whose originating agent definition is unavailable.
