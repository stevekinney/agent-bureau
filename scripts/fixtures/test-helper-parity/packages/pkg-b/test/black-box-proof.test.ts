// Fixture: the black-box test the fixture manifest's valid pairing names. A real, passing test so
// its test identifier is discoverable by the same AST scan the gate runs against real suites.
import { describe, expect, it } from 'bun:test';

describe('proves the guarantee publicly', () => {
  it('does the public thing', () => {
    expect(true).toBe(true);
  });
});
