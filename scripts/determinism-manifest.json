{
  "deterministicDirectories": [
    "packages/*/src/test/**",
    "packages/bureau/src/**/*.test.ts",
    "packages/gateway/src/conformance/**",
    "packages/integration/test/lifecycle-contract/**",
    "packages/lifecycle/src/**",
    "packages/lifecycle/test/**",
    "packages/armorer/src/**",
    "packages/skills/src/**",
    "packages/skills/test/**",
    "packages/evaluation/src/**",
    "packages/memory/src/**",
    "packages/memory/test/**",
    "packages/armorer/test/**",
    "packages/interoperability/src/**",
    "packages/conversationalist/src/**"
  ],
  "realRuntimeExemptions": [
    {
      "path": "packages/lifecycle/src/runtime-services.ts",
      "reason": "AB-278's acceptance criteria name this module as \"the one module allowed to call the real globals because it is their injection point\": createDefaultRuntimeServices() is the sole real-globals implementation of the RuntimeServices contract (AB-252). Every other module composes against the RuntimeServices interface and swaps in createManualRuntimeServices() for tests instead.",
      "owner": "Agent Bureau team",
      "owningIssue": "AB-252"
    },
    {
      "path": "packages/lifecycle/test/runtime-services.test.ts",
      "reason": "This is the direct test of createDefaultRuntimeServices() itself (the real-globals implementation) — it asserts clock.now() tracks the real Date.now(), monotonic.now() tracks real elapsed time, and timers schedule and cancel real callbacks. Injecting createManualRuntimeServices() here would test the fake instead of the real adapter it exists to prove correct.",
      "owner": "Agent Bureau team",
      "owningIssue": "AB-252"
    },
    {
      "path": "packages/lifecycle/test/manual-runtime-services-wall-clock.test.ts",
      "reason": "Split out of manual-runtime-services.test.ts (AB-292) so this exemption covers only this one assertion. It measures real performance.now() before and after a 60-second virtual advance() to prove the manual runtime's advance() does not consume real wall-clock time. Proving the absence of a real timer necessarily requires measuring against the real clock as ground truth; an injected manual clock cannot serve as its own control.",
      "owner": "Agent Bureau team",
      "owningIssue": "AB-252"
    },
    {
      "path": "packages/gateway/src/conformance/**",
      "reason": "AB-98's real-runtime conformance tier (AB-92's test-tier matrix): a real Bun HTTP/SSE/WebSocket loopback server bound to an operating-system-assigned ephemeral port, real fetch/WebSocket clients, and one deterministic bounded-drain read after a duplicate-subscription assertion (AbortSignal/ping-pong fenced, not a fixed sleep) — this tier exists specifically to exercise real sockets, which the deterministic tiers below it cannot.",
      "owner": "Agent Bureau team",
      "owningIssue": "AB-272"
    },
    {
      "path": "packages/gateway/src/ui/**",
      "reason": "Svelte hook tests assign globalThis.fetch/WebSocket/EventSource directly to install fakes (5 files: use-chat, use-reviews, use-run-detail, use-runs, use-websocket .svelte.test.ts). AB-273 injects fetch/WebSocket/EventSource/timers through a client environment so these tests stop mutating process globals.",
      "owner": "Agent Bureau team",
      "owningIssue": "AB-273"
    },
    {
      "path": "packages/lifecycle/src/identifier-seed.ts",
      "reason": "Coordinator ruling on AB-337: createManualRuntimeServices()'s default (unpinned) identifierSeed must be generated and unique per runtime instance, so two manual runtimes constructed with no explicit seed never mint colliding identifiers — concretely, the crash-conformance fixture (AB-270) re-launched as a fresh OS process after a SIGKILL (AB-321) must derive a different identifier prefix than the process it replaced. No purely deterministic function of in-process state (a module-level counter, a fixed literal) can differ across a process boundary the way this must, so this one function calls crypto.randomUUID(). A caller that needs a reproducible sequence still passes an explicit identifierSeed (AB-92, AB-263's reproduction artifact); this exemption backs only the unpinned default. Split into its own file so this exemption does not cover manual-runtime-services.ts's clock/timer/random logic, which must stay real-globals-free.",
      "owner": "Agent Bureau team",
      "owningIssue": "AB-337"
    },
    {
      "path": "packages/cloudflare/src/test/runtime-lane.ts",
      "reason": "Names a Bun.build glue entry temp file with crypto.randomUUID() so two concurrent agent-validation processes bundling the real Durable Object worker at once never write/read/race-delete the same file. No caller-injected options.identifiers value is safe here: a caller's identifier source is not guaranteed process-unique (test/cloudflare-backend-contract.test.ts uses a plain per-process counter), and this module ships as part of src/ (tsconfig.build.json rootDir is ./src), so it cannot import a default from outside src/test/ the way the *.test.ts files (excluded from the build) do.",
      "owner": "Agent Bureau team",
      "owningIssue": "AB-286"
    },
    {
      "path": "packages/bureau/src/schedule-fire.test.ts",
      "reason": "Drives weft's engine.schedule() recurring-interval poller end to end (registers a real schedule, waits for the native poller to tick, asserts the fired agent run). AB-332's spike proved the poller has no deterministic drive: runDurableMaintenance only ticks the durable-timer scheduler, not engine.schedule()'s own interval poll, so there is no manual-runtime advance() that fires it. Filed upstream as WFT-141 (Backlog) to expose a deterministic drive for the poller; each wait here is a bounded, condition-checked real-time poll (waitForReal), never a fixed sleep standing in for a check. Removing this exemption is WFT-141's consumer follow-up.",
      "owner": "Agent Bureau team",
      "owningIssue": "WFT-141"
    },
    {
      "path": "packages/bureau/src/test/harness-lmdb-isolation.test.ts",
      "reason": "AB-332 split this file out of harness.test.ts so this exemption covers only the one residual real wait AB-306's ruling named, not the whole harness suite — every other waitForRunCompletion call site in harness.test.ts now drives waitForRunState (event-driven, no real timer) instead. waitForRunCompletion's terminal-status poll needs a real per-iteration setTimeout delay, not the zero-delay MessageChannel macrotask loop waitForCondition (operative/test) and the rest of this codebase's polling uses. Measured directly against the lmdb storage fixture (AB-261): a 5,000-iteration zero-delay macrotask spin (~600ms of real time) left two concurrently-running LMDB-backed runs still 'running', while the identical scenario polled with a real setTimeout(5) between checks completed both well within a couple hundred milliseconds. A tight macrotask-only loop evidently starves the lmdb native library's own async completion callbacks rather than merely running out of iterations, so more zero-delay attempts does not fix it — only letting real wall-clock time elapse between checks does. Still a bounded, condition-checked poll (asserts the observed run status every iteration, fails loudly past its cap), never a fixed-duration sleep standing in for a real check. WFT-138 (Backlog) tracks the actual fix: a test-mode noSync/noMetaSync option on LMDBStorage so a write followed by a read in the same process observes the write without waiting on fsync; once it lands this file folds back into harness.test.ts.",
      "owner": "Agent Bureau team",
      "owningIssue": "WFT-138"
    },
    {
      "path": "packages/armorer/test/execution-lifecycle-default-runtime.test.ts",
      "reason": "Deliberately verifies createExecutionLifecycle's DEFAULT runtime argument (no injected RuntimeServices) — that it schedules deadlines with the real platform timer when the caller supplies none. Injecting a manual runtime here would defeat the point of the test. Split out of execution-lifecycle.test.ts (AB-292) so the exemption covers only this one test.",
      "owner": "Agent Bureau team",
      "owningIssue": "AB-254"
    },
    {
      "path": "packages/armorer/test/idempotency/with-idempotency-default-runtime.test.ts",
      "reason": "Deliberately verifies withIdempotency's DEFAULT prevalidation deadline scheduler (no injected runtime or setTimeoutFunction) — that it races the deadline with the real platform timer when the caller supplies none. Injecting a manual runtime here would defeat the point of the test; confirmed empirically (doing so broke the test). Split out of with-idempotency.test.ts (AB-292) so the exemption covers only this one test.",
      "owner": "Agent Bureau team",
      "owningIssue": "AB-254"
    }
  ]
}
