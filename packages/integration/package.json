{
  "name": "integration",
  "private": true,
  "type": "module",
  "scripts": {
    "transit": "bun run --cwd ../armorer build && bun run --cwd ../conversationalist build && bun run --cwd ../operative build && bun run --cwd ../sentinel build",
    "test": "bun run scripts/run-tests.ts",
    "check-types": "tsc --noEmit",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "validate": "bun run transit && bun run test && bun run check-types && bun run lint",
    "integration": "bun run validate"
  },
  "dependencies": {
    "armorer": "workspace:*",
    "conversationalist": "workspace:*",
    "operative": "workspace:*",
    "sentinel": "workspace:*",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/bun": "^1.3.9"
  }
}
