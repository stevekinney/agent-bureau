{
  "name": "integration",
  "private": true,
  "type": "module",
  "scripts": {
    "transit": "../../node_modules/.bin/turbo run build --filter=bureau",
    "test": "bun run scripts/run-tests.ts",
    "check-types": "tsc --noEmit",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "validate": "bun run transit && bun run test && bun run check-types && bun run lint",
    "integration": "bun run validate"
  },
  "dependencies": {
    "armorer": "workspace:*",
    "bureau": "workspace:*",
    "conversationalist": "workspace:*",
    "operative": "workspace:*",
    "zod": "^4.4.1"
  },
  "devDependencies": {
    "@anthropic-ai/sdk": "^0.71.2",
    "@types/bun": "^1.3.9"
  }
}
