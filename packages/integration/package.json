{
  "name": "integration",
  "private": true,
  "type": "module",
  "scripts": {
    "transit": "../../node_modules/.bin/turbo run build --filter=bureau",
    "test": "bun run scripts/run-tests.ts",
    "check-types": "tsc --noEmit",
    "format": "prettier --write \"**/*.{ts,tsx,js,jsx,mjs,cjs,json,md,yml,yaml}\"",
    "format:check": "prettier --check \"**/*.{ts,tsx,js,jsx,mjs,cjs,json,md,yml,yaml}\"",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "validate": "bun run format:check && bun run transit && bun run test && bun run check-types && bun run lint",
    "integration": "bun run validate"
  },
  "dependencies": {
    "@lostgradient/operative": "workspace:*",
    "@lostgradient/weft": "^0.23.1",
    "armorer": "workspace:*",
    "bureau": "workspace:*",
    "conversationalist": "workspace:*",
    "gateway": "workspace:*",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@anthropic-ai/sdk": "^0.116.0",
    "@types/bun": "^1.3.14"
  }
}
