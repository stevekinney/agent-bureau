# Bureau

`bureau` is the fleet composition layer for Agent Bureau. `createBureau()` assembles providers, tools, memory, skills, session persistence, durable execution, guardrails, and multi-agent behavior from one configuration surface, and exposes the resulting runtime through a single `Bureau` object.

## Table of Contents

- [Overview](#overview)
- [What It Does](#what-it-does)
- [How It Works](#how-it-works)
- [Project Role](#project-role)
- [Quick Start](#quick-start)
- [Public API](#public-api)
  - [`createBureau(options)`](#createbureauoptions)
  - [`BureauOptions`](#bureauoptions)
  - [The `Bureau` Object](#the-bureau-object)
  - [`streamEventToFrame`](#streameventtoframe)
  - [The Audit Trail](#the-audit-trail)
  - [The Durable Event History Store (AB-91)](#the-durable-event-history-store-ab-91)
- [`AgentDefinitions` and the Agent Catalog](#agentdefinitions-and-the-agent-catalog)
  - [`createSupervisor` and `createAgentDiscoveryTool`](#createsupervisor-and-createagentdiscoverytool)
- [`bureau/test`](#bureautest)
- [Development](#development)

## Overview

Everything else in the workspace is a library: `operative` runs one agent loop, `armorer` executes tools, `memory` recalls facts, `skills` catalogs procedures. `bureau` is where those libraries become a fleet — a runtime that can create runs, resume them across a crash, hold live sessions, run scheduled and durable schedules, and route requests to more than one named agent.

`gateway` is the only consumer that turns `bureau`'s runtime into an HTTP/browser product, but `createBureau()` itself has no HTTP dependency — it is usable directly from any Bun/Node process.

## What It Does

- Composes a runtime from `BureauOptions`: a single provider, a fallover/routing provider set, tools, memory, skills, session persistence, durable execution, guardrails, cache, identity, and scheduler configuration.
- Creates and tracks runs (`createRun`, **deprecated** — see AB-242), aborts and deletes them, and lists them by status.
- Persists sessions through a `SessionStore` and recovers in-flight durable runs after a process restart (`engine.recoverAll()`), reattaching bureau-owned runs and monitoring native scheduled fires.
- Exposes durable primitives when a durable engine is composed: `signalSession` delivers a real fire-and-forget signal; `updateSession`/`querySession` are kept but unconditionally throw `BureauError('UNSUPPORTED_CAPABILITY')` (AB-192 — the built-in `agentRun` workflow registers no `ctx.onUpdate`/`ctx.onQuery` handler; see `bureau.sessionVerbCapabilities`); `submitSessionInput` runs AB-42's fixed pre-admission checks (authorization, then session lifecycle, then capability/capacity) but — until a `@lostgradient/weft` version exposing WFT-84's durable mailbox is adopted — every authorized, non-terminal request unconditionally returns `{ outcome: 'unsupported-capability', reason: 'durable-mailbox-unavailable' }` (AB-194); `submitSteeringCommand` admits `pause`/`resume` steering commands on in-memory sessions the same way (AB-67/AB-199), rejecting every other target and any durably-configured session as `unsupported-capability`; plus full CRUD over durable schedules (`createSchedule`, `getSchedule`, `listSchedules`, `pauseSchedule`, `resumeSchedule`, `cancelSchedule`).
- Emits a typed event surface (`BureauEventMap`) and live WebSocket/SSE-ready frames (`ServerFrame`) via `subscribeLiveFrames` and `streamEventToFrame`.
- Records a durable, append-only audit trail of tool, run, and step lifecycle events when persistence is configured.
- Exposes a typed `AgentDefinitions` catalog (`bureau.agents`) and a synchronous `bureau.run(name, input, options?)` for dispatching to a statically named agent, alongside the session/durability-backed `createRun`.

## How It Works

`createBureau(options)` is async because composing the runtime may involve resolving a Weft storage backend, building a durable engine, and loading a session store. The returned `Bureau` object wraps an `@lostgradient/operative/store` `Store` (the live run/action registry), an optional `Memory`, an optional `Scheduler`, and the resolved runtime composition (generate function, toolbox, session store, durable engine).

`createRun(request)` (**deprecated**, not yet removed — see AB-242's decision record and the removal issue it names, AB-352) loads or creates a session, appends the request message to its conversation, builds an `ActiveRun` via `operative`'s `createActiveRun`, and registers it with the store. If a durable engine is composed, the run is routed through it instead of the in-memory loop, so it can crash and resume from its last completed step. Terminal run events (`run.completed`, `run.aborted`) persist the session's status with a bounded retry.

On boot, if durable execution is configured, `bureau` sweeps suspended scheduler-origin residue and calls `engine.recoverAll()`. Each recovered handle is classified — `reattach` (a bureau-owned, in-flight, session-confirmed run becomes a live `ActiveRun` again), `monitor` (a native scheduled fire gets a detached result monitor but no live run surface), `cancel` (a positively unowned or unidentifiable run is terminalized), or `skip` (ownership can't be confirmed, so recovery leaves it alone rather than risk cancelling a legitimate resume).

## Project Role

`bureau` sits directly below `gateway` in the dependency graph and directly above `operative`, `armorer`, `conversationalist`, `memory`, and `skills`. It depends on all five and composes them; nothing below it depends on `bureau`. `gateway` calls `createBureau()` once per server instance and layers HTTP routes, a browser UI, and live transport on top of the object it returns.

## Quick Start

This example demonstrates the `BureauOptions.provider` + `Bureau.createRun` path, which is **deprecated** (not yet removed — see AB-242's decision record and the removal issue it names, AB-352). New code should give `agents` a catalog entry built with `createAgent` and dispatch through `bureau.run(name, input, options?)` instead.

```typescript
import { createBureau } from 'bureau';

const bureau = await createBureau({
  agents: {},
  provider: { provider: 'anthropic', model: 'claude-sonnet-4.5' },
  storage: { type: 'sqlite', path: 'agent-bureau.sqlite' },
});

const run = await bureau.createRun({ message: 'Summarize the Q3 report.' });
console.log(run.status); // "running"

// Wait for completion via the live event surface.
await new Promise<void>((resolve) => {
  bureau.once('action', (event) => {
    if (event.action.runId === run.id && event.action.type === 'run.completed') {
      resolve();
    }
  });
});

const detail = bureau.getRun(run.id);
console.log(detail?.finishReason); // "stop-condition" | "maximum-steps" | …

await bureau.dispose();
```

Without `storage`, runs are ephemeral: nothing is persisted, and a crash loses in-flight work. Pass a `sqlite` or `lmdb` `storage` configuration to get session persistence and, by default, durable crash-and-resume execution.

## Public API

### `createBureau(options)`

```typescript
function createBureau<const D extends AgentDefinitions = AgentDefinitions>(
  options: BureauOptions<D>,
): Promise<Bureau<D>>;
```

Composes and returns a `Bureau`. `agents` is the only required field — pass `{}` for a bureau that only uses the session/durability-backed `createRun` surface below (**deprecated**, not yet removed — see AB-242) and doesn't dispatch through the typed catalog at all. Without a `generate`/`provider`/`providers` + `routing` (all agent-owned fields, **deprecated** per AB-242), the result has no generate function configured (`bureau.ready === false`) and `createRun` throws `BureauError('NOT_CONFIGURED')` until one is provided.

### `BureauOptions`

Every field except `agents` is optional.

| Field                                                                  | Type                                                                                                  | Purpose                                                                                                                                                                                                                                                                                                                                                                                                                               |
| ---------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `agents`                                                               | `AgentDefinitions` (`D`)                                                                              | **Required.** The typed agent catalog, exposed as `bureau.agents` and dispatched by name through `bureau.run`. Pass `{}` for a bureau that doesn't use it.                                                                                                                                                                                                                                                                            |
| `generate` **(deprecated)**                                            | `GenerateFunction`                                                                                    | Escape hatch: supply your own generate function directly, bypassing provider resolution entirely. Agent-owned now — deprecated, not removed, per AB-242; migrate to `createAgent`'s `generate`.                                                                                                                                                                                                                                       |
| `provider` **(deprecated)**                                            | `ProviderConfiguration`                                                                               | Resolve a single provider (`'anthropic' \| 'openai' \| 'gemini'`) to a generate function. Agent-owned now — deprecated, not removed, per AB-242; resolve the provider into a `GenerateFunction` and pass it as `createAgent`'s `generate` instead.                                                                                                                                                                                    |
| `providers` / `routing` **(deprecated)**                               | `ProviderRouteConfiguration[]` / `RoutingConfiguration`                                               | Resolve multiple named providers with fallover or step-based routing between them. Agent-owned now — deprecated, not removed, per AB-242; resolve the fallback/routing behavior into a `GenerateFunction` and pass it as `createAgent`'s `generate` instead.                                                                                                                                                                          |
| `toolbox` **(deprecated)**                                             | `Toolbox`                                                                                             | The bureau-level toolbox available to every run. Agent-owned now — deprecated, not removed, per AB-242; migrate to `createAgent`'s `toolbox`.                                                                                                                                                                                                                                                                                         |
| `store`                                                                | `Store`                                                                                               | Supply your own `@lostgradient/operative/store` `Store` instead of letting `bureau` create one.                                                                                                                                                                                                                                                                                                                                       |
| `persistence` / `storage`                                              | `PersistenceOptions \| StorageConfiguration \| ConditionalTextValueStore`                             | Configure session and key-value persistence. `storage` is shorthand for the common case; `persistence` is the full options-object form.                                                                                                                                                                                                                                                                                               |
| `durableExecution`                                                     | `boolean`                                                                                             | Override the default (on for persistent `storage`, off for `memory`) for Weft-backed crash-and-resume execution.                                                                                                                                                                                                                                                                                                                      |
| `memory`                                                               | `CreateMemoryOptions \| Memory`                                                                       | Attach recall/persistence hooks backed by the `memory` package.                                                                                                                                                                                                                                                                                                                                                                       |
| `cache`                                                                | `CacheConfiguration`                                                                                  | Wrap the resolved generate function with response caching.                                                                                                                                                                                                                                                                                                                                                                            |
| `guardrails` **(deprecated)**                                          | `GuardrailsOptions`                                                                                   | Attach `operative` guardrail detectors to every run. Agent-owned now — deprecated, not removed, per AB-242; AB-15's end state moves this per-agent onto `createAgent`, but that field has not landed on `CreateAgentOptions` yet — see AB-242 and AB-352.                                                                                                                                                                             |
| `identity`                                                             | `IdentityConfiguration`                                                                               | Configure identity resolution for memory and session ownership.                                                                                                                                                                                                                                                                                                                                                                       |
| `skills` **(deprecated)**                                              | `SkillRuntimeConfiguration`                                                                           | Attach a skill catalog; injects an `<available_skills>` system block on step 0 of every run. Agent-owned now — deprecated, not removed, per AB-242; AB-15's end state moves this per-agent onto `createAgent`, but that field has not landed on `CreateAgentOptions` yet — see AB-242 and AB-352.                                                                                                                                     |
| `streaming`                                                            | `StreamingConfiguration`                                                                              | Enable enhanced streaming and expose a `streamEventTarget` on run runtime.                                                                                                                                                                                                                                                                                                                                                            |
| `scheduler`                                                            | `SchedulerConfiguration`                                                                              | Attach an in-process priority scheduler (`submitSchedulerTask`).                                                                                                                                                                                                                                                                                                                                                                      |
| `stopWhen` **(deprecated)**                                            | `StopCondition \| StopCondition[]`                                                                    | Default stop condition(s) applied to every run. Agent-owned now — deprecated, not removed, per AB-242; migrate to `createAgent`'s `stopWhen`.                                                                                                                                                                                                                                                                                         |
| `maximumSteps` / `systemPrompt` **(deprecated)**                       | `number` / `string`                                                                                   | Default step cap and system prompt for runs that don't specify their own. Agent-owned now — deprecated, not removed, per AB-242; migrate to `createAgent`'s `maximumSteps`/`instructions`.                                                                                                                                                                                                                                            |
| `observability`                                                        | `boolean \| Omit<ObservabilityOptions, 'eventTarget'>`                                                | Opt into OpenTelemetry spans/metrics for durable runs. No-op without `@opentelemetry/api` installed.                                                                                                                                                                                                                                                                                                                                  |
| `onLog`                                                                | `(record: WorkflowLogRecord) => void`                                                                 | Sink for `ctx.log` records emitted by durable workflows.                                                                                                                                                                                                                                                                                                                                                                              |
| `onDiagnostic`                                                         | `(diagnostic: BureauDiagnostic) => void`                                                              | Sink for bureau's own operational diagnostics — recovery failures, live-frame listener exceptions, dispose errors, persistence failures. Omit to log to the console exactly as before.                                                                                                                                                                                                                                                |
| `durableGuardrails`                                                    | `DurableGuardrailsConfiguration`                                                                      | History/checkpoint guardrails (max events, checkpoint size warnings) for durable runs.                                                                                                                                                                                                                                                                                                                                                |
| `durableOwnership`                                                     | `Pick<CreateRunEngineOptions, 'ownership' \| 'workflowClaimTtlMs' \| 'workflowClaimRenewIntervalMs'>` | Multi-process ownership posture (AB-178). Omit for the default `'none'` — unchanged, one bureau process per store. See the caveat below before passing `'workflow-lease'`.                                                                                                                                                                                                                                                            |
| `sessionPersistenceRetryDelayMilliseconds` / `sessionPersistenceSleep` | `number` / `(ms: number) => Promise<void>`                                                            | Tune the bounded retry used when persisting terminal session state.                                                                                                                                                                                                                                                                                                                                                                   |
| `sessionInput`                                                         | `{ sessionBacklogLimit?: number; principalBacklogLimit?: number }`                                    | AB-42/AB-194 — per-session/per-principal backlog caps for `submitSessionInput`. Each is validated as a positive integer at construction time (`BureauError('...', 'BAD_REQUEST')` otherwise); `DEFAULT_SESSION_INPUT_BACKLOG_LIMIT` (32) and `DEFAULT_PRINCIPAL_SESSION_INPUT_BACKLOG_LIMIT` (128) are the values a future mailbox-backed admission path will apply for an omitted field — neither cap is enforced yet.               |
| `modelCatalog`                                                         | `ModelCatalogService`                                                                                 | AB-246 — the started-work-conformant model-catalog refresh service exposed as `bureau.modelCatalog`. Omit to let `createBureau` build a default service seeded from `@lostgradient/operative/providers`'s static `createModelCatalog()`; pass one explicitly to share a catalog across bureaus or supply a non-default `descriptorSource`. See [`model-catalog-refresh.ts`](./src/model-catalog-refresh.ts).                          |
| `modelPolicy`                                                          | `{ deployment?; bureau?; users?: Record<string, UserModelConfiguration>; policyRevision: number }`    | AB-64/AB-250 — deployment/Bureau model-policy invariants and the per-principal user configuration `bureau.planSelection`/a run's `SelectionGate` compose against, held in memory for the bureau's lifetime. Named policy profiles are keys of `users` and of each configuration's own `fallbackOrder`; there is no storage schema. `BureauRunOptions` gains no field for any of this. See [`model-policy.ts`](./src/model-policy.ts). |

`durableExecution: true` cannot be combined with a custom `persistence` value — `persistence` shadows `storage`, and the durable engine needs a raw `Storage` backend to checkpoint against. Provide `storage` without `persistence` for durable runs.

> [!WARNING] `durableOwnership: { ownership: 'workflow-lease' }` and scheduler preemption
> Do not enable `'workflow-lease'` if you rely on the `scheduler` option's preemption behavior — a background task that gets suspended and later resumed. A reproduced weft 0.23.1 defect makes `'workflow-lease'` incompatible with same-engine `engine.suspend()`/`engine.resume()`, which Bureau's scheduler uses internally for that exact suspend-and-resume flow. Enabling it on a bureau that also configures `scheduler` can cause a preempted background task to fail on resume instead of continuing. See `CreateRunEngineOptions.ownership`'s JSDoc in `@lostgradient/operative/durable` for the full repro and root cause.

`onLog` and `onDiagnostic` cover different sources: `onLog` carries only `ctx.log` records emitted _by durable workflow code itself_, while `onDiagnostic` carries bureau's own operational diagnostics — the sites that write to `console.error`/`console.warn` today (recovery failures, live-frame listener exceptions, dispose errors, persistence failures). Route `onDiagnostic` into a structured logger to capture those without monkeypatching `console`:

```ts
import pino from 'pino';

const logger = pino();

const bureau = await createBureau({
  agents: {},
  onDiagnostic: ({ level, scope, message, cause }) => logger[level]({ scope, cause }, message),
});
```

### The `Bureau` Object

The most commonly used members:

| Member                                                                                                                                          | Returns                                                                                                                                                                                                                                                    | Purpose                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| ----------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `agents`                                                                                                                                        | `BureauAgentCatalog<D>`                                                                                                                                                                                                                                    | The typed, read-only agent catalog. See [`AgentDefinitions` and the Agent Catalog](#agentdefinitions-and-the-agent-catalog).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `modelCatalog`                                                                                                                                  | `ModelCatalogService`                                                                                                                                                                                                                                      | AB-246 — the model-catalog refresh service, present regardless of `D`. `dispose()` awaits any in-flight refresh before reporting rather than aborting it.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `planSelection(request)`                                                                                                                        | `SelectionPlan`                                                                                                                                                                                                                                            | AB-64/AB-250 — builds a full `SelectionPlan` for `request` against this bureau's CURRENT model catalog and `BureauOptions.modelPolicy` configuration. Synchronous and side-effect-free: starts no run, refreshes no catalog, dispatches no event. `request.principal` selects the per-principal `UserModelConfiguration`, the same value `BureauRunOptions.principal` carries for an actual run.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `run(name, input, options?)`                                                                                                                    | `AgentRun<...>`                                                                                                                                                                                                                                            | Synchronous dispatch to a named catalog agent.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `createRun(request)` **(deprecated)**                                                                                                           | `Promise<RunSummary>`                                                                                                                                                                                                                                      | Create and start a run against a session. Deprecated, not removed, per AB-242; migrate to `bureau.run(name, input, options?)` with a catalog `RunnableAgent`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `submitSchedulerTask(request)`                                                                                                                  | `Promise<SubmitSchedulerTaskResponse>`                                                                                                                                                                                                                     | Queue a task on the in-process scheduler.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `listRuns(status?)`                                                                                                                             | `RunSummary[]`                                                                                                                                                                                                                                             | List tracked runs, optionally filtered by status.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `getRun(id)` / `abortRun(id)` / `deleteRun(id)`                                                                                                 | `RunDetail \| undefined` / `RunSummary` / `void`                                                                                                                                                                                                           | Read, abort, or remove a tracked run. `RunDetail` carries a `liveness: LivenessSnapshot` field (AB-88/AB-214) — the run's current liveness observed at call time. `abortRun` is idempotent (AB-37/AB-205): called against a run that is not currently `'running'` it returns that run's current summary instead of throwing `CONFLICT`. Called against a running run it returns synchronously with `status: 'aborting'` — a transitional value, never `'aborted'`, since teardown has only just been requested — cleared to the run's real terminal status once its own terminal event settles. `getRun`/`listRuns` deliberately keep reporting the run's real, unmodified status (still `'running'` at that point) rather than this transitional value, so `status !== 'running'` keeps meaning "settled" everywhere else that idiom is used (e.g. `operative/test`'s `waitForRunState`). `listAbortingRuns(): readonly { runId: string; since: number }[]` (AB-369) publicly reads the same in-flight-abort bookkeeping `abortRun` maintains internally — every run whose cancellation has been requested but whose `ActiveRun.closed()` has not yet settled, paired with the `RuntimeServices.clock.now()` timestamp cancellation was first requested. `bureau/test`'s `BureauQuiescenceReport` surfaces a non-empty entry here as a non-quiescent report at shutdown.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `subscribeRunSnapshot(runId, observer, options?)`                                                                                               | `Subscription`                                                                                                                                                                                                                                             | Live liveness updates (AB-88/AB-214), delegating to the run's `ActiveRun.subscribeSnapshot`. Delivers the current snapshot synchronously, then a new one on every revision change. Throws `NOT_FOUND` for an unknown `runId`, matching `abortRun`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `getDurableRun(runId)` / `listDurableRuns(...)`                                                                                                 | `Promise<WorkflowState \| null \| undefined>` / `Promise<PaginatedResult<WorkflowSummary> \| undefined>`                                                                                                                                                   | Read the durable engine's own view of a run. `undefined` without a durable engine.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `cancelDurableRun(runId)`                                                                                                                       | `Promise<CancelDurableRunOutcome>`                                                                                                                                                                                                                         | Cancel a durable run that has no live, process-local `ActiveRun` (AB-37/AB-205) — the boot-recovered fire that makes `abortRun` throw `NOT_FOUND`. Asynchronous, unlike `abortRun`, and never rejects: resolves `'unsupported-capability'` with no durable engine composed, `'not-found'` for an unknown `runId`, `'already-terminal'` for a workflow already outside the forcibly-terminable statuses (without calling the engine's `cancel`), `'requested'` only once a post-cancel re-read confirms the workflow actually reached `'cancelled'` (never merely because the engine's `cancel` call resolved — it resolves `void` unconditionally whether it won or lost a race against normal completion), or `'failed'` with the error attached.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `waitForRecovery()`                                                                                                                             | `Promise<BureauRecoveryReport>`                                                                                                                                                                                                                            | AB-349 — waits for boot durable recovery to finish and resolves a typed report: `outcome: 'clean' \| 'partial' \| 'failed'`, plus `sweepFailure`/`batchFailure` (each `{ message }`) and `perRunFailures` (`{ runId, reason }[]`, one entry per handle whose verdict was `'cancel'`). `outcome` is `'failed'` when `batchFailure` is set (`durable.engine.recoverAll()` itself threw — no handle was classified at all); otherwise `'partial'` when `sweepFailure` is set or `perRunFailures` is non-empty; otherwise `'clean'`. `'skip'`/`'reattach'`/`'monitor'`/`'reattach-version-mismatch'` verdicts never change `outcome`. Recovery failure stays diagnostic either way — `createBureau()` never rejects on it, in whole or in part; this is the typed alternative to parsing `onDiagnostic`'s `scope: 'recovery'` log lines. **Boot returns once recovery has STARTED, not completed**, and what `await waitForRecovery()` buys a caller differs by path: with NO deferred authority validator (the common case), `createBureau()` itself already awaits this same barrier before resolving, so by the time a caller has a `bureau` to call `waitForRecovery()` on, recovery classification is already done and a recovered run may already be advancing — calling it here only reports that already-settled fact, and a caller with post-boot dependency wiring a recovered run needs must finish that wiring BEFORE calling `createBureau()` instead, there is no after-the-fact seam on this path; WITH a deferred authority validator (the standard `createBureau()` then `createGateway()` sequence), recovery does not start at all until `setRequestAuthorityValidator()` is called, so wiring dependencies before or immediately after that call and then awaiting `waitForRecovery()` genuinely gates on recovery's classification/reattachment pass completing.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `listSessions()` / `getSession(id)` / `deleteSession(id)`                                                                                       | `Promise<SessionSummary[]>` / `Promise<AgentSession \| undefined>` / `Promise<void>`                                                                                                                                                                       | Session CRUD through the configured `SessionStore`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `signalSession(id, name, payload?)`                                                                                                             | `Promise<void>`                                                                                                                                                                                                                                            | Fire-and-forget signal to a session's in-flight durable run.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `updateSession(id, name, payload?)`                                                                                                             | `Promise<unknown>`                                                                                                                                                                                                                                         | Unconditionally throws `BureauError('UNSUPPORTED_CAPABILITY')` (AB-192): the built-in `agentRun` workflow registers no `ctx.onUpdate` handler.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `querySession(id, name, input?)`                                                                                                                | `Promise<unknown>`                                                                                                                                                                                                                                         | Unconditionally throws `BureauError('UNSUPPORTED_CAPABILITY')` (AB-192): the built-in `agentRun` workflow registers no `ctx.onQuery` handler.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `submitSessionInput(sessionId, request)`                                                                                                        | `Promise<SessionInputAdmissionOutcome>`                                                                                                                                                                                                                    | AB-42/AB-194 — admits session input. An unauthorized caller or unknown session returns `{ outcome: 'not-found' }`; an authorized caller on an already-terminal session returns `{ outcome: 'session-terminal', sessionId }`; every other authorized, non-terminal request returns `{ outcome: 'unsupported-capability', reason: 'durable-mailbox-unavailable' }` until WFT-84's durable mailbox is adopted — no `SessionInputRecord` is created by this method today.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `submitSteeringCommand(sessionId, request)`                                                                                                     | `Promise<SteeringCommandAdmissionOutcome>`                                                                                                                                                                                                                 | AB-67/AB-199 — admits a `pause`/`resume` steering command, scoped to an in-memory session. Same `not-found`/`session-terminal` pre-admission checks as `submitSessionInput`. Every other target, and `pause`/`resume` against a `runtime.durable`-configured bureau, returns `{ outcome: 'unsupported-capability', reason: 'selector-unavailable' \| 'durable-steering-unavailable' }`. A `pause` increments `configVersion` by exactly one and is idempotent while already paused; a `resume` against an unpaused session is an idempotent no-op; an exact `(principal, id)` retry replays; a mismatched retry returns a typed `conflict`; an accepted pause/resume transitions to `failed`/`'run-terminal'` if its bound run ends first.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `sessionVerbCapabilities`                                                                                                                       | `{ signal: true; update: false; query: false }`                                                                                                                                                                                                            | Constant, synchronous capability discovery for the three throw/no-throw session verbs (AB-192); `submitSessionInput`/`submitSteeringCommand` are distinct, always-present verbs not covered by this map.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| `createSchedule(definition)` / `getSchedule(id)` / `listSchedules(filter?)` / `pauseSchedule(id)` / `resumeSchedule(id)` / `cancelSchedule(id)` | various                                                                                                                                                                                                                                                    | Durable recurring schedule CRUD, backed by the Weft engine. `createSchedule` dispatches `AgentScheduledEvent` (`schedule.created`, AB-298, `@lostgradient/operative`) on this bureau's own event surface exactly once per successful registration — forwarded straight through by passing this bureau's own emitter into `createAgentScheduler`, not dispatched a second time by `create-bureau.ts` the way the pause/resume/cancel events below are. `pauseSchedule`/`resumeSchedule`/`cancelSchedule` each dispatch `SchedulePausedEvent`/`ScheduleResumedEvent`/`ScheduleCancelledEvent` (AB-223, `@lostgradient/operative`) on this bureau's own event surface exactly once per successful call. `schedule.deleted` is reserved and unreachable — there is no distinct delete surface; `DELETE /schedules/:id` on the gateway routes through `cancelSchedule`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `getConfiguration()` / `getTools()`                                                                                                             | `ConfigurationResponse` / `ToolSummary[]`                                                                                                                                                                                                                  | Introspect the resolved provider/tool configuration.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `subscribeLiveFrames(listener)`                                                                                                                 | `() => void`                                                                                                                                                                                                                                               | Subscribe to every `ServerFrame` the bureau emits; returns an unsubscribe function.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `addEventListener` / `on` / `once` / `subscribe` / `toObservable` / `events`                                                                    | —                                                                                                                                                                                                                                                          | The full `lifecycle`-style event surface over `BureauEventMap`: `action`, `run.registered`, `run.removed`, `bureau.disposed`, plus (AB-298) `schedule.created` from `createSchedule` — carries `agentName`, `scheduleId`, and the `spec`/`sessionId` summary, dispatched exactly once per registered schedule and never on a shared heartbeat's re-registration — (AB-223) `schedule.paused`/`schedule.resumed`/`schedule.cancelled` from `pauseSchedule`/`resumeSchedule`/`cancelSchedule`, and `schedule.completed`/`schedule.failed` — a scheduled fire's terminal outcome, correlated to the fired run via `runId`, distinct from that run's own `run.completed`/`run.error`, dispatched exactly once whether the fire settled live or was recovered on restart. AB-90/ab90-09 adds `recovery.attempted`/`recovery.rejected`/`recovery.lease-released`: `recovery.attempted` fires every time `classifyRecoveredRun` classifies a recovered handle during boot recovery, carrying `runId` and the verdict (`'reattach' \| 'reattach-version-mismatch' \| 'monitor' \| 'cancel' \| 'skip'`); `recovery.rejected` fires immediately after, only for the positive-rejection (`'cancel'`) verdict, carrying `runId` and a structured `reason` (`'metadata-read-failed' \| 'foreign-input' \| 'session-absent' \| 'session-run-mismatch' \| 'session-not-running'`); `recovery.lease-released` fires when `Engine.getLeaseHealth()` reports a released engine-level lease overlapping the pass, carrying `runId` and a `LivenessLeaseEvidence` payload projected from Weft's own evidence — never a Bureau-owned lease (AB-39). AB-224 adds the typed `review.*` lifecycle family — one event per non-`'pending'` `ReviewStatus` value (`review.approved`/`denied`/`rejected`/`expired`/`revoked`/`canceled`/`superseded`), dispatched from `recordReviewDecision`/`recordReviewStatusTransition` alongside their existing durable audit write; `pending` itself dispatches nothing. Each carries only `reviewId`/`runId`/`principal`/`kind` — never the review's own approval arguments, signal payload, or decision reason, which stay privileged to the durable audit trail per AB-87's redaction column. `review.revoked` (`'system:run-deletion'`/`'system:session-deletion'`, from `deleteRun`/`deleteSession`) is distinct from `review.canceled` (`'system:run-abort'`, from `abortRun`/`cancelDurableRun`) — the two never fire for the same triggering call; `review.expired` is `'system:expiry-sweep'` from `sweepExpiredReviews`; `review.superseded` is `'system:supersession'` from `resolveReview`'s `resumeApproval` re-gate. |
| `ready`                                                                                                                                         | `boolean`                                                                                                                                                                                                                                                  | Whether a generate function is configured.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| `sessionStore` / `kv` / `auditTrail` / `eventHistory` / `subscribeEventHistory`                                                                 | `SessionStore \| undefined` / `ConditionalTextValueStore \| undefined` / `AuditTrail \| undefined` / `(owner, options?) => Promise<DurableEventPage \| DurableEventGap \| EventHistoryUnsupportedOutcome>` / `(owner, listener, options?) => Subscription` | The underlying persistence handles, when configured; `eventHistory` pages one run's or session's durable event history, `subscribeEventHistory` replays then live-tails it with no gap or duplicate at the handoff (AB-91/AB-311).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `shutdown(options?)`                                                                                                                            | `Promise<BureauShutdownReport>`                                                                                                                                                                                                                            | AB-207 — the awaited drain. `{ policy: 'abort' }` (the default) aborts every active run immediately, then awaits every Bureau-owned subsystem's teardown (scheduler, online-evals, webhook notifier, audit trail, durable engine); `{ policy: 'drain' }` lets every caller-owned run (`bureau.run`/session-owned) reach its own natural terminal result while Bureau-owned background work is stopped identically to `'abort'`. `timeoutMilliseconds` bounds the WAIT only — on elapse, `shutdown()` resolves (never rejects) reporting every still-outstanding owner `'unresolved'`; the underlying drain is not abandoned, only the wait for it.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `dispose()`                                                                                                                                     | `Promise<void>`                                                                                                                                                                                                                                            | Thin wrapper over `shutdown({ policy: 'abort' })`, kept for its existing `Promise<void>` shape. Tears down subscriptions and closes owned storage handles — genuinely awaited now (AB-207), not fire-and-forget.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |

`bureau.dispose()` (and `bureau.shutdown()`) is safe to call more than once — the second call returns the same, already-settled promise rather than re-running teardown.

**The manual-runtime rule (AB-338):** `shutdown()`'s bounded `timeoutMilliseconds` wait always sleeps on this bureau's own resolved `RuntimeServices.timers` (AB-260's deterministic contract), never a real timer directly. Under a real-globals runtime that is invisible — the wait just elapses. Under a `ManualRuntimeServices`, nothing advances that wait's timer on its own, so the CALLER owns advancing the clock (`runtime.advance(timeoutMilliseconds)`) far enough for a bounded `shutdown()` call to resolve, exactly as it owns advancing the clock for any other timer-driven behavior in that bureau. The one built-in exception is `bureau/test`'s `BureauTestHarness.close(shutdownOptions)` (below), which owns that advance itself.

### `streamEventToFrame`

```typescript
function streamEventToFrame(runId: string, event: StreamEvent): ServerFrame | undefined;
```

Converts an `operative` `StreamEvent` (`stream:text-delta`, `stream:tool-call-start`, `stream:tool-call-delta`, `stream:tool-call-complete`, `stream:complete`, `stream:error`) into the matching typed `ServerFrame`, stamped with the owning `runId`. Returns `undefined` for event types with no frame equivalent. `bureau` uses this internally to translate a run's `streaming` output into the frames delivered by `subscribeLiveFrames`; a WebSocket or SSE transport layer (like `gateway`'s) can use it the same way.

### The Audit Trail

```typescript
import { createAuditTrail } from 'bureau';

const trail = createAuditTrail(bureau, kv);
const records = await trail.query({ runId, since: Date.now() - 3_600_000, limit: 100 });
await trail.dispose();
```

`createBureau()` builds an audit trail automatically whenever persistence is configured — it's exposed as `bureau.auditTrail`. It listens to the bureau's `action` events and, for a fixed set of event types (`tool.started`, `tool.settled`, `tool.error`, `run.completed`, `run.error`, `run.aborted`, `run.tripwire`, `step.completed`, plus, as of AB-228, `budget.exceeded` (the bare `BudgetExceededEvent` class has no production emitter — AB-231, merged, reclassifies to `run.completed`'s `finishReason` instead), and the toolbox-forwarded `toolbox.loop-warning`/`toolbox.loop-blocked`/`toolbox.budget-exceeded` (the toolbox's OWN real budget rejection, a distinct production path the bare `budget.exceeded` entry does not cover — Codex review finding, PR #566) — see `AUDIT_EVENT_TYPES`), writes an append-only `AuditRecord` into the key-value store under an `audit:v1:` prefix, key-encoded so natural sort order is chronological. AB-228 also added dedicated listeners for the schedule-DEFINITION lifecycle (`schedule.created`/`paused`/`resumed`/`cancelled`) and for `session.deleted`, none of which traverse `'action'` at all (same reason `durable-event-history.ts`'s own producer needs its own listeners for the schedule family) — `deleteSession` dispatches `SessionDeletedEvent` on the bureau emitter (there was previously no production emission point at all, despite `session.deleted` sitting in `SESSION_DURABLE_ACTION_TYPES` — Codex review finding, PR #566), replayed from the session commit outbox by `drainOutbox` rather than dispatched directly from `deleteSession` itself as of AB-389 (see "Session commit outbox" below), and this trail records it under a synthetic `session:<sessionId>` owner, the same convention `schedule:<scheduleId>` uses, since neither a schedule definition nor a session deletion has a run of its own to attribute to. The seven `review.*` `ReviewStatus` outcomes are NOT in `AUDIT_EVENT_TYPES` at all: they already reach this trail through `AuditTrail.record()`'s out-of-band path (`create-bureau.ts`'s `recordReviewDecision`/`recordReviewStatusTransition`, kind-namespaced as `review.<kind>.<status>`), which bypasses the allowlist entirely — see `AUDIT_EVENT_TYPES`'s own doc comment for the full accounting.

This is a second, durable layer alongside the in-memory `@lostgradient/operative/store` ring buffer (which is bounded by `maxActions` and lost on restart): the operative store is the live/glass-box view, the audit trail is the durable/queryable one. `trail.query(options)` filters by `since`, `runId`, and `type`, returning up to `limit` records (default 500) oldest-first. Without a `kv` store, the trail still subscribes (so `dispose()` is always safe) but writes nothing. Every write is fire-and-forget from its dispatching caller's perspective (`pauseSchedule`/`resumeSchedule`/`cancelSchedule`/`deleteSession` all return once the synchronous event dispatch completes, not once the underlying `kv.set` commits), so `query()` first awaits a snapshot of every write already in flight at call time (AB-228) — giving read-your-writes for a query immediately following a schedule transition or session deletion, even against a `TextValueStore` whose `set()` resolves asynchronously.

`encodeAuditEntryKey(timestampMs, sequence, runId)` (also exported from `bureau`) reconstructs the exact `audit:v1:...` key a queried `AuditRecord` was written under from its own public fields — used by `bureau/test`'s `selectAuditWriteFaultTarget` (AB-263) rather than a second, drifting reimplementation of the key encoding.

`trail.dispose()` returns `Promise<void>` (AB-207) — it resolves only after every write already in flight has settled, and accepts an owner-issued `AbortSignal` (threaded in by `bureau.shutdown()`) that tells the trail to stop _starting new writes_ once aborted; a write already in flight when the signal fires cannot be cancelled (there is no cancellation hook for the underlying `kv.set`), so `dispose()` still fully awaits it rather than racing the KV store's own close. `bureau.shutdown()`/`bureau.dispose()` await this the same way they await the webhook notifier and online-eval sampler's own async `dispose()`.

#### `AuditRecord` schema versioning (AB-370)

`AuditRecord.sequence` is one monotonic counter shared by every write path this trail has — the action-stream listener above AND every out-of-band write (`record()`, the schedule-definition listeners, the `session.deleted` listener) all draw the next value from the same per-bureau counter, allocated at the moment a write is dispatched (before its `kv.set` even starts). `createBureau()` seeds that counter above the highest `sequence` already persisted under `audit:v1:` at boot (`computeInitialAuditSequence`, also exported from `bureau`), so a fresh process never re-issues a value a prior process lifetime already used. Readers order by `timestampMs` first and `sequence` second: `sequence` only breaks a genuine same-millisecond tie, so a query's order for two records that do not share a millisecond is unaffected by this field. Before AB-370, an action-stream record's `sequence` was a straight copy of the operative store's own per-process `Action.sequence`, and an out-of-band record's `sequence` was a second, deliberately huge and disjoint counter (`writeOutOfBandRecord`'s old `manualSequence`, seeded near `Number.MAX_SAFE_INTEGER`) — two numbering domains that could never be compared for true relative order, so a same-millisecond collision between (for example) `session.deleted` and a released run's own terminal action always resolved in the out-of-band record's favor regardless of which one genuinely happened first. That bias is gone: both paths now draw from the identical counter, so a same-millisecond tie sorts by true dispatch order.

`sequence` is optional on `AuditRecord` — a record written before this field existed at all carries none, and `query()`'s sort comparator maps that absence to `-1` (below every real `sequence`, which is always `>= 0`) so a same-millisecond tie against such a record still uses a genuinely transitive comparator, rather than the `0`-for-either-side special case an earlier round of this fix used (non-transitive, and not guaranteed to produce the claimed order by every sort algorithm).

`AuditRecord.actionSequence` is a separate field, present only on records the action-stream listener writes: it preserves the original operative-store `Action.sequence` this record was sunk from, since that value is no longer copied into `sequence`. The gateway's `GET /api/v1/audit` route (`packages/gateway/src/routes/audit.ts`) reads `actionSequence`, not `sequence`, to dedup its merged live+durable view against the live store's own `action.sequence` — an out-of-band record (`session.deleted`, `schedule.*`, `review.*`) never has a live action counterpart, so it never carries `actionSequence` and never needs a dedup key.

#### Audit trail retention (AB-388)

`BureauOptions.auditRetention` controls whether the durable audit trail is ever pruned. It defaults to `'forever'` — unbounded, exactly the behavior described above — so a long-lived deployment must opt in before any postmortem evidence is ever removed:

```typescript
const bureau = await createBureau({
  // ...
  auditRetention: { olderThan: 30 * 24 * 60 * 60 * 1000 }, // 30 days
});
```

`options.auditRetention.olderThan` must be a finite, non-negative number — validated at `createBureau()` construction time (`BAD_REQUEST` otherwise), since `NaN` or a negative value feeding the cutoff arithmetic could otherwise prune far more than intended (Codex review, PR #597, "Reject non-finite and negative retention durations").

`retentionFloorTimestamp()`'s protection depends on a schedule/session dedicated event's durable-event `emittedAtMs` never diverging from that SAME event's own audit record `timestampMs` — `createAuditTrail` and `createDurableEventProducer` share one `emittedAtMs` resolver (`createEventTimestampResolver`, `event-timestamp.ts`), keyed by event-object identity, so whichever of the two subsystems' listeners for a `schedule.created`/`paused`/`resumed`/`cancelled` or `session.deleted` event runs first stamps a single clock reading that the other reuses, instead of each independently calling `runtime.clock.now()` and risking two different readings for the exact same event (Codex review, PR #597, "Reuse event timestamps for dedicated lifecycle listeners").

With `{ olderThan: milliseconds }` configured, a pruning pass deletes any audit record whose `timestampMs` is older than `olderThan` by the runtime clock — but never a record at or after the Weft fleet feed's own retention floor (`DurableEventHistory.retentionFloorTimestamp()`: the earliest `emittedAtMs` across every durable event the feed currently retains), even when `olderThan` alone would have allowed pruning it sooner. That clamp means audit evidence always outlives the durable events it describes: as long as the fleet feed still retains a run's own durable history, that run's audit records survive too, regardless of how old they are by the wall clock. Critically, this protection applies even while the floor is still `0` — nothing has ever been retired from the fleet feed yet is the normal, common state (`FleetEventFeed.retain()` is never called automatically; see below), not "nothing to protect" (Codex review, PR #597, "Preserve audit records while the fleet feed floor is zero"). The effective cutoff each pass uses is `min(now - olderThan, retentionFloorTimestamp)`, or just `now - olderThan` when the feed currently retains no events at all (an ephemeral bureau, a persistent backend with no durable event history, or one where every retained event has aged out). The global floor timestamp alone cannot express "protect this specific run's ENTIRE audit history" when that run's only currently-retained durable event is a single, later terminal transition (`run.completed`/etc.) — a pass also asks `DurableEventHistory.retainedRunOwnerIds()` for the full set of run ids the fleet feed still retains at least one event for, and never prunes a record whose `runId` names one of those runs, regardless of `timestampMs` (Codex review, PR #597, "Protect earlier audit records for retained owners"). Reading the floor and the retained-owner set both waits for any `DurableEventProducer` write already in flight for the SAME action first (`waitForAllActiveWrites()`), so an audit record that already committed can never have its protection computed from a snapshot that omits the still-in-flight durable event describing that same action (Codex review, PR #597, "Wait for durable event writes before reading the floor").

Retention also reaches a KV-backed bureau with no durable engine at all — a persistent storage backend configured with `durableExecution: false`, or a custom KV-only `persistence` value (`ConditionalTextValueStore`) — not only one with `runtime.durable` composed (Codex review, PR #597, "Run audit retention for KV-backed non-durable bureaus"); such a bureau has no durable event history to consult, so its effective cutoff is always the unclamped `now - olderThan`. The automatic-profile background timer itself only starts for a KV-only bureau when `auditRetention` is actually active (not the default `'forever'`) — with no durable engine and no active retention policy, that bureau has nothing for the timer to do, so it is never started at all rather than ticking uselessly forever (Codex review, PR #597, "Start the KV-only timer only for active retention"); a durable bureau's timer still always starts regardless of `auditRetention`, since `pruneStaleRunOwnership` has its own, independent work to do.

Pruning is never driven on a schedule of its own — it runs inside the SAME durable maintenance pass `lastRunOwningPrincipals` pruning already uses (AB-363, described under "The Durable Event History Store" below): `bureau.runDurableMaintenance()` for a `durableBackgroundTasks: 'manual'` host's own alarm/Cron trigger, and, under the default `'automatic'` profile, the same background interval that already prunes stale run ownership every five minutes. The two sub-passes are independent — each runs under its own try/catch, on BOTH the manual and automatic paths, so a failure in one (diagnosed under its own message) never prevents the other from running that tick (Codex review, PR #597, "Isolate the manual audit-retention sub-pass"). A rejected `engine.runMaintenance()` call is isolated the same way: `runDurableMaintenance()` still runs both `pruneStaleRunOwnership()` and `pruneAuditTrail()` even when the engine's own maintenance rejects, re-throwing the captured engine failure only after both sub-passes have run, so a failed alarm no longer silently also skips audit retention (Codex review, PR #597, "Isolate audit pruning from engine maintenance failures"). Every manual `runDurableMaintenance()` call still in flight is tracked and awaited by `shutdown()` before backend teardown, alongside the automatic timer's own in-flight pass — a manual host's own alarm/Cron trigger racing `shutdown()` can no longer have its storage disposed mid-prune (Codex review, PR #597, "Await manual pruning before storage teardown").

A pass that actually prunes at least one record writes exactly one `audit.pruned` record back into the trail itself, `detail: { count, cutoffMs }`, naming how many records it removed and the effective cutoff it used; a pass that prunes nothing writes no record at all, so an idle retention policy never grows the trail it exists to bound. This summary write is strict (its failure rejects the pass, rather than the best-effort swallow every other out-of-band audit write uses) and bypasses `dispose()`'s "refuse new writes" abort check for a pass that was already admitted before `shutdown()` began (Codex review, PR #597, "Propagate failures to persist the prune summary" / "Finish the prune summary after shutdown begins") — permanently deleting records without the promised evidence, or losing that evidence to a mid-shutdown abort, would be worse than surfacing the failure. Each pass also persists the highest `sequence` it ever pruned — BEFORE deleting any record (Codex review, PR #597, "Persist the sequence watermark before deleting records"), so a crash partway through a pass never loses track of a sequence it already started removing — under a separate, non-`audit:v1:`-prefixed key, so it is invisible to `query()` and to the normal boot scan; `computeInitialAuditSequence` consults that persisted floor in addition to its usual scan, so a restart's shared sequence counter resumes correctly even when every one of the surviving records at boot postdates every pruned one (or nothing at all survived). Two overlapping `prune()` calls on the same `AuditTrail` instance (e.g. a manual host's own call landing mid-automatic-tick) are serialized through an internal queue rather than racing each other's reads and deletes (Codex review, PR #597, "Serialize concurrent audit-pruning passes"). A cross-process race between two Bureau instances sharing the same KV store is closed separately, via a CAS-guarded lease (`ConditionalTextValueStore.conditionalBatch`): only the instance whose lease-acquisition batch commits first actually lists, deletes, and updates the watermark for that tick, and every other instance skips the tick entirely rather than racing its own reads and deletes against the holder's — a lease older than its own TTL (a prior holder that crashed before releasing it) is taken over rather than blocking pruning forever (Codex review, PR #597, "Coordinate pruning across shared-store instances"). If `kv.delete()` rejects partway through a multi-record pass, the records deleted before the failure are still named in a partial `audit.pruned` summary (`detail.partial: true`) before the failure is re-thrown, so an already-committed deletion is never left with no corresponding evidence (Codex review, PR #597, "Record successful deletions when a later delete fails").

### The Durable Event History Store (AB-91)

```typescript
const page = await bureau.eventHistory({ kind: 'run', id: runId });
if ('outcome' in page) {
  // 'unsupported-capability' (ephemeral bureau) or 'gap' (since predates retention)
} else {
  console.log(page.events, page.hasMore, page.nextCursor);
}
```

`createBureau()` builds a second durable store — `packages/bureau/src/durable-event-history.ts` — over Weft's `FleetEventFeed` (`@lostgradient/weft@^0.23.1`) whenever a genuinely **persistent** storage backend is configured (SQLite/LMDB/etc — `capabilities().persistence !== 'ephemeral'`; forcing `durableExecution: true` over `{ type: 'memory' }` does NOT compose one, since the backend itself is still ephemeral). It shares `runtime.durable.engine.storage` — no second durable log — and is exposed as `bureau.eventHistory(owner, options?)`, a bounded, sequence-ordered read over one run's or one session's durable events after an exclusive `since` cursor, or a typed `DurableEventGap` when `since` predates the store's retention floor, and `bureau.subscribeEventHistory(owner, listener, options?)`, a race-free replay-then-live subscription over the same events (AB-311). An ephemeral bureau's `eventHistory()` call resolves `{ outcome: 'unsupported-capability', reason: 'no-persistent-storage' }`; its `subscribeEventHistory()` call returns an already-closed `Subscription` that never delivers.

`eventHistory(owner, { principal })` authorizes a `{ kind: 'run', ... }` owner against the run's dispatch-time `principal` and fails CLOSED (`{ outcome: 'not-found' }`) whenever that ownership cannot be verified — including for a run that was never dispatched with a principal, and for a deleted run — rather than treating an unresolved entry as open (AB-313). This holds across a process restart: the owning principal recorded at `createRun` time is persisted on the run's owning session (`lastRunOwningPrincipals`, alongside the existing per-run `lastRequestAuthorities` AB-285 already writes there) and rehydrated into the same in-memory attribution a live run uses the moment a durably recovered run is reattached — so a recovered run's real owner reads its durable history exactly as it did before the crash, and a run genuinely dispatched without a principal stays exactly as unresolved as it was live (AB-359). Omitting `principal` entirely still bypasses the check for every owner kind (an internal/trusted caller).

This is a THIRD layer, distinct from both the operative store (Layer A, live) and the audit trail above (Layer B, KV-based, `AUDIT_EVENT_TYPES`-scoped — see that section for the exact set): the event history store is restart-durable by construction (proven by reopening the same SQLite/LMDB file across two independently constructed store instances — see `durable-event-history.test.ts`), owner-scoped rather than a flat log, and cursor/gap-aware for reconnect-safe paging.

A producer (`createDurableEventProducer`, wired into `createBureau()` alongside the audit trail) sinks the run/session/schedule-fire/schedule-definition families AB-87's matrix classifies as durable into `record()` — `run.completed`/`error`/`aborted`/`tripwire` (owner: the action's `runId`) and the durable `session.*` lifecycle rows (owner: the action detail's `sessionId`) from the bureau's own action stream; `schedule.completed`/`schedule.failed` (a scheduled fire is "an ordinary run" per AB-87, so these are recorded under the fired run's own `{ kind: 'run', id: runId }` owner) and, as of AB-320, `schedule.created`/`paused`/`resumed`/`cancelled` (recorded under `{ kind: 'schedule', id: scheduleId }`, the schedule's own owner kind) from the bureau's own schedule-lifecycle events. A schedule's durable page carries its four definition events only — never a fire; the fired run's own page carries the fire only — never a definition event. AB-224 adds a fourth source, the same shape as `schedule.*`: the seven `review.*` lifecycle events (`review.approved`/`denied`/`rejected`/`expired`/`revoked`/`canceled`/`superseded`), recorded under the reviewed run's own `{ kind: 'run', id: runId }` owner with a minimal, privileged payload (`reviewId`/`runId`/`principal`/`kind`). AB-372 adds a fifth, dedicated listener rather than another `'action'`-stream case: `deleteSession` dispatches `SessionDeletedEvent` on the bureau emitter (never through the action stream — as of AB-389, replayed from the session commit outbox by `drainOutbox` rather than dispatched directly by `deleteSession` itself; see "Session commit outbox" below), so this producer listens for it directly and records the same `{ kind: 'session', id: sessionId }` / `session.deleted` durable event `Bureau.eventHistory`'s deleted-aggregate detection reads — closing the gap where that detection was previously only ever exercised by synthetic records the test suite constructed directly. It de-duplicates a same-process duplicate dispatch (concurrent `deleteSession` calls racing to delete the same session) with an in-flight `Map` keyed by owner AND `AgentSession.incarnation` (AB-384 — before it, the key was owner alone, which conflated a session id recreated and deleted again WHILE the prior incarnation's own write was still pending with a duplicate dispatch of that prior deletion; see `SessionDeletedEvent`'s own doc comment), checked and set synchronously before any `await`; it does not scan the durable page for a prior record, so a session recreated under the same id and deleted again writes a second, later `session.deleted` row rather than being suppressed as a duplicate — the two dispatches are for different incarnations, not the same one, and now produce two records even when their writes overlap. This producer also exposes `waitForActiveWrites(owner): Promise<void>`, a snapshot-only sibling to `hasActiveWrite`: it awaits every write in flight for that owner AT THE MOMENT OF THE CALL (never one started afterward) and never rejects, so `resolveEventHistory` can await it before paging to close the read-your-writes race against this producer's own fire-and-forget writes without risking an unrelated caller's write leaving it hanging.

AB-384 adds a sixth and seventh source, closing a dead entry each: `SESSION_DURABLE_ACTION_TYPES` has listed `session.created`/`session.saved` since AB-91, but nothing ever dispatched either onto the `'action'` stream. `@lostgradient/operative`'s `SessionStore` dispatches both on the bureau-level emitter — as of AB-389, replayed from the session commit outbox, never dispatched directly by `save()`/`update()` themselves (see "Session commit outbox" below) — and this producer's two dedicated listeners record them under the same `{ kind: 'session', id: sessionId }` owner `session.deleted` already uses. Every one of these three listeners' `history.record()` calls now also passes an `options.dedupeKey` (the outbox entry's own `ordinal`, stringified) and `options.emittedAtMs` (the entry's own `committedAtMs`, so a delayed replay never misdates the event as happening "now") — never embedded in the recorded `payload` itself, so a caller's own payload carrying a same-named `dedupeKey` field is unaffected. `record()` writes the dedupe identity as an atomic storage compare-and-swap alongside the event append itself (in the SAME `conditionalBatch` `FleetEventFeed.append` commits), not a read-before-write scan: two processes racing to record the identical `(owner, kind, dedupeKey)` can never both succeed, closing the redelivery race a scan alone cannot (a crash between a durable write settling and the entry being acknowledged, or two drains racing over one shared backend, resolves to a no-op read of the winner's own record, never a second durable append).

### Session commit outbox (AB-389)

`SessionStore.save()`, `update()`, and `delete()` (`@lostgradient/operative`) no longer dispatch `SessionCreatedEvent`/`SessionSavedEvent`/`SessionDeletedEvent` directly — that fire-and-forget dispatch, immediately after each commit but not transactionally coupled to it, was a known limitation (AB-384, PR #592): a crash between a commit succeeding and its listener's write running lost the fact forever, and two `SessionStore` instances sharing one persistent backend could record events out of true commit order. Each commit now appends a `SessionOutboxEntry` — payload, incarnation, and the store's own monotonically increasing commit ordinal — to the SAME atomic batch as the write it describes, and dispatches only a best-effort `SessionOutboxAppendedEvent` drain trigger.

`drainOutbox` (`create-bureau.ts`) is the ONE place those entries are replayed as the real events, in ordinal order: it reads `sessionStore.outbox.pending()` and, for each entry, first calls `sessionStore.outbox.claim(ordinal, { owner, until })` (AB-390) — a compare-and-swap on the entry's own `claim` field, keyed by this bureau instance's own drain-owner id (minted once via `RuntimeServices.identifiers.next()`) and `RuntimeServices.clock.now()`. Only a WON claim proceeds to dispatch the matching event onto the bureau-level emitter (isolated in its own try/catch — a throwing `bureau.toObservable()` subscriber is diagnosed and skipped, never allowed to leave the reserved lowest-ordinal entry permanently un-acknowledged and starve every later fact behind it); a claim that fails (a different, still-unexpired owner holds it) stops the WHOLE pass at that entry rather than skipping ahead to a later ordinal, preserving true commit order across drainers. After dispatch, it awaits `DurableEventProducer.waitForActiveWrites(owner)` for the durable write that dispatch triggers, then verifies the write actually SUCCEEDED — `waitForActiveWrites` resolving proves only that it settled — via `eventHistoryInstance.wasRecorded(owner, kind, dedupeKey)` (an O(1) storage read, skipped only when no durable event history is configured) and, for a `session.deleted` entry specifically, via a read-your-writes `auditTrail.query({ runId })` against the out-of-band audit record (skipped only when no KV persistence is configured) — and only once every configured verification passes does it call `sessionStore.outbox.acknowledge(ordinal, owner)`, which itself re-verifies this same owner still holds the claim before removing the entry. A failed verification, or a lost claim at acknowledge time, leaves the entry pending for a later drain to retry, rather than discarding it. Three drivers call this same function: the post-commit `SessionOutboxAppendedEvent` trigger (promptness only — tracked via `RuntimeServices.deferred`, so a test can `await runtime.deferred.drain()` for it deterministically), `runDurableMaintenance()` and the automatic run-ownership-pruning timer (both "on the runtime clock inside the durable maintenance pass" — the timer's own pending pass is awaited, and the pass's chained drain settles, BEFORE `shutdown()` aborts the durable event producer's signal, so a drain still in flight when disposal begins can still durably record what it dispatches), and `recoverDurableRuns()` (boot recovery, so a crash between a commit and its own drain is caught up before this bureau serves reads) — plus an authority-independent catch-up pass that runs unconditionally at the very start of boot, even when durable run reattachment itself is deferred pending a request-authority validator attachment. It is single-flighted per bureau instance — a caller invoking it while a drain is already running gets the SAME promise, and a join landing exactly as that drain is about to complete forces one more full pass before the promise resolves, so the post-commit trigger and an overlapping maintenance tick never both drain the same pending entry from within one process, and no join is ever silently dropped at the completion edge. A lost claim schedules its own retry (a single-slot timer, never more than one outstanding) armed at the CURRENT winning claim's own `until` — read directly off `outbox.claim()`'s own result, never off the pass's earlier `pending()` snapshot, which a peer's intervening `claim()` call could already have made stale — so a KV-only or manual-maintenance bureau still recovers an entry once the blocking lease expires, rather than waiting on an unrelated future trigger. Shutdown clears that retry timer and, because a timer whose callback already started running before the clear cannot be un-fired, also awaits any drain already in flight (whether from that retry or any other driver) before the durable event producer's signal is aborted.

`performDeleteSession` additionally calls `drainOutbox()` directly, right after `sessionStore.delete()` commits and gated on that call having actually removed a live record — "immediately after each commit" for the one call site that can know synchronously that its own commit just happened, so a caller awaiting `deleteSession()` observes the durable `session.deleted` record without needing to wait for the next trigger or maintenance tick.

**Claim lease across processes (AB-390):** the AB-389 residual — `pending()`/`acknowledge()` claiming nothing exclusively across bureau PROCESSES sharing one persistent backend — is closed by the claim described above. The maintenance-timer and boot-recovery drains no longer both dispatch the same still-unacknowledged entry: whichever process's `claim()` wins the compare-and-swap proceeds; the other's `claim()` fails and its pass stops there, picking the entry back up on a later tick once the current claim either acknowledges it or its lease lapses. A drainer that crashes between winning a claim and acknowledging leaves the entry claimed but undelivered only until its own lease's `until` passes — a later drain (this same process's next tick, or a peer's) reclaims and completes it exactly once, closing the AB-389 residual where `audit-trail.ts`'s `session.deleted` listener and a direct `bureau.addEventListener('session.deleted', ...)` consumer could each see a replay twice.

Two follow-on mechanisms close gaps a fixed lease alone would leave open (Codex P1 review findings, PR #599). First, a renewal timer runs for the ENTIRE stretch between winning a claim and acknowledging — the durable-write wait, `wasRecorded`, and the audit-trail read-your-writes query have no upper bound under real backpressure, so a claim taken once, before any of that, could otherwise lapse mid-wait and let a peer reclaim and dispatch the SAME entry again before this drainer ever discovers the loss. Second, when `claim()` loses to a still-live peer, this bureau arms a single-slot timer at that peer's own known `claim.until` (read directly off the `pending()` snapshot) rather than depending on whichever unrelated trigger happens to fire next — a KV-only or manual-maintenance bureau with no other commits arriving would otherwise have no way to notice the blocking claim expired. The drain-owner id itself is minted from `RuntimeServices.identifiers.next()` plus `crypto.randomUUID()`, not the runtime sequence alone — two independently constructed manual/deterministic runtimes with the same `identifierSeed` are documented to intentionally mint identical sequences (exact reproduction is the point of that seam), which would otherwise let two real bureau processes collide on the same drain-owner value and treat each other's claims as same-owner renewal.

### Review-transition audit records ride the outbox (AB-391)

`resolveReview`'s approve/deny/reject decisions, and the `reviewResolutionCleanupPending` retry path for a decision whose session-store commit previously failed, used to write their durable `review.<kind>.<approved|denied|rejected>` audit record via a SEPARATE, fire-and-forget `AuditTrail.record()` call AFTER the session-store commit (`persistReviewResolution`) that marks the review resolved. A crash between the two lost the audit record forever: the session commit already marks the review resolved, so nothing ever retries the write.

`SessionStore.update()` (`@lostgradient/operative`) now accepts an `options.outbox` list of `{ namespace, payload }` attachments, appended as `SessionOutboxEntry` values of kind `'session.attachment'` in the SAME `conditionalBatch` as the update's own body/summary/outbox-ordinal writes, at the ordinals immediately following the update's own `session.created`/`session.saved` entry. `persistReviewResolution` passes the pending decision's audit payload (`{ runId, type, detail, principal }` — exactly `AuditTrail.record()`'s own input shape) under the `'audit-record'` namespace. `drainOutbox` (generalized from `drainSessionOutbox`, which drained only session-lifecycle entries) replays a `session.attachment` entry into the real audit trail via `AuditTrail.record()`, stamped with the entry's own `committedAtMs` so a delayed replay never misdates the record, and verifies the write against the audit trail (by `runId`/`type`/`timestampMs`/`detail` identity) BEFORE acknowledging — the only idempotency guard here, since `record()` always mints a fresh key and a duplicate replay would otherwise produce a second, distinct record rather than a no-op. `resolveReview` calls `drainOutbox()` itself immediately after the resolution commit succeeds, so the audit record (and `getReview`'s audit-trail reconstruction of it) is available without waiting for a later trigger.

This coupling covers exactly the transitions the fix targets — a review's terminal decision. `expired` (`sweepExpiredReviews`), `revoked`/`canceled` (`revokePendingApprovalsForRun`), and `superseded` (`resolveReview`'s tool-approval re-gate branch) have no preceding session-store commit to couple to (the first two never persist a session-store write on that path at all; `superseded`'s own preceding commit, `persistPendingApprovalOverrideWithRetry`, writes the REPLACEMENT approval, and bundling the superseded review's own audit payload — which embeds its now-revoked ORIGINAL approval token — into that same batch would put both tokens in one commit, a real behavioral difference, not just an implementation detail) — all three keep writing their audit record directly, exactly as before.

`SessionStore.update({ outbox })` is explicitly session-agnostic (any caller can append any `namespace`), so `drainOutboxPass` checks a `session.attachment` entry's namespace BEFORE claiming it: an unrecognized namespace is left pending and, critically, never claimed at all — claiming an entry this bureau will never acknowledge would only extend its lease on every maintenance tick (a same-owner `claim()` renews an existing lease), starving a different consumer that DOES recognize the namespace. The drain pass filters that ordinal out of its own remaining work for the rest of the pass so a later, recognized ordinal still drains in the same pass rather than the whole pass looping on the unrecognized entry forever. A payload under this bureau's OWN `'audit-record'` namespace that fails shape validation is a different case — there is no future version of that namespace to defer to, so it is diagnosed and acknowledged without replay, same as always. The attachment replay's idempotency is `AuditTrail.record`'s own `dedupeKey` (Codex review finding, PR #601, "Make attachment replay deduplication atomic") — `session.attachment:<sessionId>:<ordinal>` — mirroring `durable-event-history.ts`'s own `dedupeKey` (see above): an atomic storage-level compare-and-swap, not a read-before-write existence query, so two Bureau processes both reclaiming this entry after a claim-lease renewal race can never both durably write it, and the write stays correct regardless of how much unrelated history shares the same `runId`/`type` (a read-scoped-by-`AuditTrail.query()`'s-default-`limit` check would not). `record()` rejects on a genuine storage failure when `dedupeKey` is supplied — unlike its default best-effort path — so `drainOutboxAttachmentEntry` leaves a failed write's entry claimed-but-unacknowledged for a later drain to retry rather than losing it. `resolveReview` and the cleanup-retry path both isolate this post-commit drain call in its own try/catch (Codex review finding, PR #601, "Isolate post-commit drain failures from review resolution"): a transient drain failure diagnoses and leaves the attachment pending, but never prevents the already-succeeded resolution from returning its result or dispatching its live event. `persistReviewResolution` reports whether its `sessionStore.update()` call actually committed (Codex review finding, PR #601, "Detect when the resolution update appends nothing") — `false` when the session vanished concurrently (the updater then returns `undefined`, appending neither the resolution nor the audit attachment) — so `recordReviewDecision` falls back to its own direct write instead of wrongly assuming the coupled write already covered it. Finally, `persistReviewResolution`'s own updater guards against an AMBIGUOUS commit failure (Codex P1 review finding, PR #601, "Avoid reattaching audits after an ambiguous commit") — the fault-engine's own `fail-after-commit` boundary names exactly this: `conditionalBatch` durably commits the resolution and its coupled attachment, but the call back still rejects (a lost acknowledgement). Retrying blindly (either `persistReviewResolutionWithRetry`'s own in-loop retry, or a wholly separate later call from the `reviewResolutionCleanupPending` path) would call `persistReviewResolution` again, which unconditionally appends ANOTHER `session.attachment` entry at a fresh ordinal — a distinct `dedupeKey` from the one the ambiguous attempt already committed, so the same decision would be recorded twice once both drained, even though the `dedupeKey` mechanism correctly prevents either individual entry from being replayed on its own. So the updater itself detects this on every attempt: if `reviewId` is already present in the CAS-fresh session's `resolvedReviewIds`, some earlier commit already durably landed this exact decision, and the updater returns `undefined` — `sessionStore.update()` never calls `commit()` at all, appending nothing more — while `persistReviewResolution` still reports success.

Two more fixes to the `dedupeKey` mechanism itself, both in `audit-trail.ts`. First, `writeOutOfBandRecord`'s `dedupeKey` branch now calls `trackWrite` (Codex P2 review finding, PR #601, "Track deduplicated audit writes before returning") — a non-rejecting derivative of the `conditionalBatch` promise, so `query()`, `runtime.deferred.drain()`, and `dispose()` all observe this write the same as every other path, while the REJECTING `conditionalBatch` promise itself is still what a `dedupeKey` caller awaits. Second, `prune()` now reconciles orphaned `audit-dedupe:v1:` markers (Codex P2 review finding, PR #601, "Prune dedupe markers with expired audit records") — every `record({ dedupeKey })` write leaves one of these permanently, and nothing previously deleted them, so retention bounded the audit records themselves but not this marker family. `runPrunePassLocked` lists every marker each pass (matching its own existing cost profile, which already lists every audit-record key each pass regardless of the retention floor) and deletes any whose named record no longer exists — a true RECONCILIATION rather than a filter over just this pass's own deleted candidates, so it also heals a marker orphaned by an EARLIER pass crashing between deleting a record and deleting its marker, which could never be re-discovered by matching against a fresh candidate list. Runs even on the "nothing qualified" early-return path, and is isolated in its own try/catch — a marker-cleanup failure never turns an otherwise-successful record prune into a rejected pass, and marker deletions are never counted in `prunedCount` or the `audit.pruned` summary, which describe audit records, not markers. `durable-event-history.ts`'s own `outbox-dedupe:` marker family has no equivalent cleanup — a separate, tracked gap (see `followUps`), since that file's pruning story is independent of this one.

`lastRunOwningPrincipals` is never pruned on a run's terminal transition, and never on a count (AB-363's coordinator ruling): pruning on either signal would reintroduce the exact denial AB-359 fixed for a run whose durable history is still pageable long after it completed. The only pruning is `pruneStaleRunOwnership`, which drops an entry once its run's ENTIRE durable event history has fallen below the fleet feed's retention floor, because a history that can no longer be paged needs no owner. It runs on every `bureau.runDurableMaintenance()` call (the manual/serverless host's own driver) AND, under the default `durableBackgroundTasks: 'automatic'` profile, on its own in-process interval — Weft's engine already runs its own `engine.runMaintenance()` automatically in that profile, but exposes no hook to run extra work after each cycle, so Bureau starts a second, `pruneStaleRunOwnership`-only interval (never re-invoking `engine.runMaintenance()` itself) rather than leaving pruning unreachable for the default profile (Codex review, PR #568, "Run ownership pruning in the automatic maintenance profile"). This timer is injectable-clock-aware (`RuntimeServices.timers`), and a failed pass is diagnosed rather than thrown. A tick that fires while the prior pass is still running skips starting a second one rather than overlapping them (Codex/Copilot review, PR #579, "Prevent overlapping automatic pruning passes"); `shutdown()`/`dispose()` both clear the interval AND await a pass still in flight before disposing the event history and storage it reads and writes (Codex review, PR #579, "Await running pruning passes before backend teardown"). The in-flight pass is tracked in a plain local variable, not `RuntimeServices.deferred` — nothing in production ever calls `deferred.drain()`, so tracking a recurring five-minute tick there would grow that ledger unboundedly for the life of the process (Codex review, PR #579, "Bound deferred tracking for recurring passes").

A candidate run is excluded from pruning — left for a later cycle — on any of three signals, never a separate durable-engine read: its `lastRequestAuthorities` entry still being present (still live, or terminal-but-awaiting-a-pending-approval-decision, which can still produce a future `review.*` durable event — a value present but NOT a plain record fails the WHOLE session closed rather than being read as "no live runs", Codex review, PR #568, "Preserve owners when the authority map is malformed"); `DurableEventProducer.hasActiveWrite({ kind: 'run', id })` reporting a `history.record()` write for it still in flight — closing the window between a run's terminal transition removing its authority entry and that SAME transition's own durable write actually committing (Codex/Copilot review, PR #568); or the run still being present in the in-memory run store (`store.getRun(runId) !== undefined`) — `deleteRun()` remains callable on any such run at any future point, and records a brand-new, retained `run.removed` durable event under this SAME owner (Codex review, PR #568, "Preserve ownership for a later run removal event"). That third signal is why pruning a still-live process's own runs is rarer in practice than the retention floor alone suggests: nothing in this codebase evicts a run from the store except an explicit `deleteRun()` call, so a completed run that is never deleted stays protected for the lifetime of the process that dispatched it, however far the floor advances past its history — pruning only reaches it after an explicit deletion (whose own `run.removed` then also ages past the floor) or on a later process's maintenance pass, since a terminal run is never reattached into the store at boot (only in-flight ones are), which is also why that later process needs no protection for a run it never held: `deleteRun()` throws `NOT_FOUND` for a run absent from its own store, so no future `run.removed` is possible for it there either.

The retained-owner set itself is revalidated fresh immediately before each session's write (inside `SessionStore.update`'s callback, re-run on every conflict retry, and passed `{ refreshActivity: false }` so this maintenance write never itself reads as session activity — Codex review, PR #568, "Avoid refreshing session activity during retention pruning") — a run can dispatch, complete, and have its terminal event land in the feed entirely within the time this pass spends paging through other sessions, which would make a single up-front snapshot stale by the time that run's own session is reached (Codex review, PR #568, "Revalidate retained owners before pruning"). That revalidation is a cheap, cursor-resumed `DurableEventHistory.refreshRetainedRunOwnerIds()` call, not a second from-scratch `retainedRunOwnerIds()` full replay per candidate session — the latter would turn one maintenance pass into O(candidate sessions × retained events) storage reads (Codex review, PR #568, "Avoid replaying the full fleet feed per candidate session"); the initial snapshot (one full replay) is still taken once, up front, per pass. `refreshRetainedRunOwnerIds()` also mutates that ONE snapshot's owner set in place rather than cloning it on every call — a per-candidate, per-retry `Set` copy of every retained owner would have kept CPU/allocation cost at O(candidate sessions × retained owners) even after the storage-read cost above was fixed (Codex review, PR #579, "Stop copying the full owner set on every refresh"). This narrows, but does not fully close, a genuinely cross-process race — another Bureau instance's write committing after this process's fresh check but before this write lands — since no primitive here spans both the fleet feed and the session store in one atomic operation; tracked as a `followUps` item rather than solved in this pass. Nothing in this codebase calls `FleetEventFeed.retain()` today, so in practice this pass is a no-op until an operator or a future retention driver advances the floor.

```typescript
const subscription = bureau.subscribeEventHistory(
  { kind: 'run', id: runId },
  (event) => console.log(event.kind, event.payload),
  { since: lastKnownCursor },
);
// later
subscription.unsubscribe();
```

`subscribeEventHistory` replays every event after `since`, then transitions to live delivery with no gap and no duplicate at the handoff, built entirely on `FleetEventFeed.subscribe()` — Weft's own race-free replay-then-tail composition — so this module adds no sequencing of its own. Disposing one subscription (`unsubscribe()`, or aborting `options.signal`) stops delivery to that listener only; a second, independent subscription to the same owner is unaffected, and the underlying `FleetEventFeed` is released only when the whole store's `dispose()` runs (Gateway's paging/SSE/WebSocket projection — AB-91's `ab91-03` — remains a separate, not-yet-built slice).

`bureau.shutdown()`/`bureau.dispose()` dispose the producer, then this store (an `'event-history'` `BureauShutdownOwnerReport` row) in the same drain ordering as the audit trail, before the durable engine and raw storage are torn down.

## `AgentDefinitions` and the Agent Catalog

`AgentDefinitions` (AB-15, AB-22) is a plain literal object — the `agents` map passed to `createBureau({ agents })` — of agent name to `RunnableAgent` (an `@lostgradient/operative` `createAgent()` or `createLazyAgent()` result). There is no register/unregister lifecycle: the map is fixed for the bureau's lifetime. `bureau.agents` exposes an immutable, read-only `BureauAgentCatalog` view over it (`get`/`find`/`has`/`names`/`entries`/`query`), and `bureau.run(name, input, options?)` dispatches to a named entry synchronously — like `RunnableAgent.run`, it returns the `AgentRun` handle immediately, never `Promise<AgentRun>`.

```typescript
import { createAgent } from '@lostgradient/operative';
import { createBureau } from 'bureau';

const bureau = await createBureau({
  agents: {
    researcher: createAgent({ generate, instructions: 'You are a research assistant.' }),
    writer: createAgent({ generate }),
  },
});

bureau.agents.has('researcher'); // narrows to a known agent name where TypeScript permits it
bureau.agents.names(); // ['researcher', 'writer'] — definition order

const run = bureau.run('researcher', 'Summarize the Q3 report.');
for await (const event of run) {
  // ...
}
const result = await run.result();
```

`run`'s synchronous throws are limited to an unknown `name`, a disposed bureau, and malformed `input`/`options`; everything else (session, provider, tool, policy, or abort failures) settles through the returned `AgentRun` handle. `BureauRunOptions` (`sessionId?`, `signal?`, `traceContext?`, `withTraceContext?`, `principal?`) carries only call-scoped concerns — there is no per-call `systemPrompt`/`maximumSteps`/`maximumTokens` override; anything that shapes how an agent runs is fixed on its own definition (`createAgent({ instructions, maximumSteps, ... })`). `sessionId` correlates a durable dispatch to the engine's own session/recovery bookkeeping (a no-op when the run is dispatched in-memory); `principal` (AB-241) forwards into `AgentRunContext.principal` on both dispatch branches — the direct branch hands it straight to the agent's own `run()`, and the durable branch additionally records it exactly as `Bureau.createRun` does (`LivenessSnapshot.owner`, and the in-memory `runAttribution` map `eventHistory`'s principal gate consults) — so a caller sees the same attribution regardless of which branch a given agent takes. A non-string value throws synchronously (`BureauError` `BAD_REQUEST`).

`run` is independent of, and additive to, `createRun` below (`createRun` and the bureau-level `generate`/`provider`/`providers`/`routing`/`toolbox`/`guardrails`/`skills`/`stopWhen`/`maximumSteps`/`systemPrompt` fields it drives are all **deprecated**, not yet removed — see AB-242's decision record and the removal issue it names, AB-352): `run` dispatches to a catalog `RunnableAgent` (agent-owned generate/tools/durability by construction), while `createRun` keeps driving bureau-level `generate`/`provider` through the session/durable-execution machinery documented above. A bureau may use either, both, or neither — pass `agents: {}` for a bureau that only uses `createRun`. When a durable engine is composed (a persistent `storage` backend, or `durableExecution: true`) and the named agent supports AB-21's definition-resolution capability (every `createAgent`/`createLazyAgent` result does), `run` drives the dispatch through that same durable engine so it survives a crash and resumes; otherwise the agent's own in-memory `run()` is used directly.

### `createSupervisor` and `createAgentDiscoveryTool`

Both moved here from `operative` (AB-22), rebuilt against `BureauAgentCatalog<D>` in place of the deleted `AgentRegistry`.

```typescript
import { createAgentDiscoveryTool, createFanOutRouting, createSupervisor } from 'bureau';

const supervisor = createSupervisor({
  agents: bureau.agents,
  routing: createFanOutRouting(), // or a custom `RoutingStrategy<D>`
});

const { agentResults, synthesis } = await supervisor.delegate('Draft a Q3 summary.');

const discoveryTool = createAgentDiscoveryTool(bureau.agents);
```

`createSupervisor` delegates a task to one or more catalog agents chosen by a pluggable `RoutingStrategy<D>` (`(task, descriptors) => name | name[] | Promise<...>`; built-ins `createRoundRobinRouting`/`createFanOutRouting` only load the agents they actually select) and synthesizes their results — `delegate` (one task), `delegateAll` (many, sequential or parallel), and `pipeline` (chain each stage's output into the next stage's input). `createAgentDiscoveryTool` exposes a tool an orchestrating agent can call to search catalog agent names (case-insensitive substring match) — metadata only, since `RunnableAgent` carries no description/capabilities/tags to search beyond its name.

## `bureau/test`

```typescript
import {
  createBureau,
  createBureauTestHarness,
  createLmdbStorageFixture,
  createMemoryStorageFixture,
  createSqliteStorageFixture,
  waitForCondition,
  waitForRunState,
} from 'bureau/test';
```

Re-exports `createBureau` (identical to the top-level export — useful when a test file already imports other test utilities from this subpath) alongside `@lostgradient/operative/test`'s `waitForCondition` and `waitForRunState`, which poll a run/condition without a fixed sleep.

### `createBureauTestHarness(options)` (AB-261)

Composes a real `Bureau` over an injected `ManualRuntimeServices` (`options.runtime`, defaulting to a freshly constructed one) and an owned `BureauStorageFixture` (`options.storage`, required). The returned promise resolves only once `bureau.ready` is `true` and boot recovery has completed. Every other `BureauOptions` field passes through unchanged.

```typescript
import { createAgent } from '@lostgradient/operative';
import { createBureauTestHarness, createSqliteStorageFixture } from 'bureau/test';
import { createManualRuntimeServices } from 'lifecycle';

const runtime = createManualRuntimeServices();
const storage = createSqliteStorageFixture({ runtime });

const harness = await createBureauTestHarness({
  agents: { worker: createAgent({ generate: myGenerate }) },
  runtime,
  storage,
});

const run = harness.startRun('worker', 'hello');
await run.unwrap();

await harness.bureau.dispose();
await storage.dispose();
```

The harness exposes `bureau`, `runtime`, `storage`, `scope`, and `childRegistry` directly, plus thin lifecycle drivers over the corresponding public `Bureau` method — `startRun` (`Bureau.run`, and registers the returned root onto `scope`), `startSession` (`Bureau.createRun`), `startChild` (dispatches through `bureau.agents` and operative's public `dispatchChildRun`, correlated to a caller-supplied `parentRunId`, registering into `childRegistry` by default unless `options.registry` overrides it), `submitSchedulerTask`, `createRecurringSchedule` (`Bureau.createSchedule`), `resolveReview`, `deliverSignal` (`Bureau.signalSession`), and `reattachDurable` (`Bureau.getDurableRun`). None of them reach a private map or a raw Weft workflow handle.

`harness.supports(capability)` reports `false` for `'managed-goal'` and `'scheduler-task-result'` — product surfaces that don't exist on this baseline yet (AB-101/AB-102 and AB-180, respectively). Calling `harness.startManagedGoal()` or `harness.getSchedulerTaskResult()` throws a typed `BureauHarnessUnsupportedError` naming the owning issue(s) rather than silently no-oping.

Two harnesses constructed independently in one process — each with its own `runtime`/`storage` — share no state: their clocks, timers, identifier sequences, storage paths, and events are fully isolated.

`harness.registerDurableRun(runId, { detached? })` and `harness.close()` are AB-262's slice — see "Quiescence report and `close()`" below. Out of scope for this harness: packed-consumer tarball verification (AB-264).

### Quiescence report and `close()` (AB-262)

```typescript
import { assertBureauQuiescent, BureauQuiescenceError } from 'bureau/test';
```

`harness.close(shutdownOptions?): Promise<BureauQuiescenceReport>` calls `bureau.shutdown(shutdownOptions)` (AB-207) — via `assertBureauQuiescent(harness, shutdownOptions)` — disposes `harness.storage`, then resolves with the report, or rejects with a `BureauQuiescenceError` (its `.report` carries the full structured detail) when anything Bureau owns was not quiescent. It never substitutes `bureau.dispose()` for `shutdown()`, and it is idempotent: a second call returns the exact same settled outcome without shutting down twice (a second call's own `shutdownOptions` are ignored, matching `bureau.shutdown()`'s own idempotency contract).

**The manual-runtime rule (AB-338):** `BureauOptions.shutdownTimeoutSleep` — the sleep `shutdown({ timeoutMilliseconds })`'s bounded wait races against — always stays driven by the bureau's own resolved `RuntimeServices.timers` (AB-260's deterministic contract); it never falls back to a real timer under a `ManualRuntimeServices`. That means nothing advances that sleep's timer on its own: under a plain `bureau.shutdown({ timeoutMilliseconds })` call against a manually-driven bureau, the caller owns advancing the clock (`runtime.advance(timeoutMilliseconds)`) far enough for the wait to elapse, exactly as it already owns advancing the clock for any other timer-driven behavior. `harness.close(shutdownOptions)` is the one exception: when `shutdownOptions.timeoutMilliseconds` is set, the harness itself advances its own `ManualRuntimeServices` by that many milliseconds once `bureau.shutdown()` has armed the timer — so a test calling `harness.close({ policy: 'drain', timeoutMilliseconds })` against a hung run never needs a real wall-clock wait or a manual `runtime.advance()` call of its own to observe a bounded drain resolve with the hung work reported under `incomplete`.

`BureauQuiescenceReport` extends `@lostgradient/operative/test`'s `QuiescenceReport` (`scope`, `quiescent`, `leaked`, `detached`) with ten Bureau-owned `readonly LeakedResource[]` rows: `activeRoots`/`activeDescendants` (from `harness.scope`/`harness.childRegistry`), `pendingWebhookDeliveries` (`webhookNotifier.listDeliveries()`, status `'pending'`), `durableAttempts` (`bureau.getDurableRun` for every id registered via `harness.registerDurableRun`, when non-terminal and not detached). `parkedWaits` and `pendingHookEffects` stay empty on this baseline — no public per-signal-wait listing or hook-effect tracking surface exists yet (AB-35, Backlog).

**`abortingRuns` (AB-369):** a `readonly { runId: string; since: number }[]` row, sourced from `bureau.listAbortingRuns()` — the public read over the same `abortingRunIds` bookkeeping `abortRun` maintains internally to guard against re-aborting a run it already requested cancellation for. `since` is the `RuntimeServices.clock.now()` timestamp `abortRun` recorded when cancellation was first requested. Unlike the ten rows above, a non-empty `abortingRuns` DOES fold into `quiescent` directly (`quiescent = leaked.length === 0 && abortingRuns.length === 0`) rather than through `leaked`: a run whose `ActiveRun.closed()` never settles after `abortRun` requested cancellation stays listed here for the life of the process, which is exactly the leak this row exists to surface. `BureauQuiescenceError`'s message names each entry under an `abortingRuns:` heading when the report is non-quiescent for this reason.

The other four — `runningScheduleFires`, `activeEvaluations`, `pendingAuditWrites`, `openStorageResources` — read empty on the common path (nothing genuinely survives an unbounded `bureau.shutdown()`, which awaits each owner's real drain) but populate for real once AB-265's fault engine forces a genuine leftover past a bounded `shutdown({ timeoutMilliseconds })` (AB-322): `runningScheduleFires` from `bureau.scheduler.getState().activeTask`, `activeEvaluations` from `bureau.onlineEvalSampler.activeEvaluationSnapshots()`, `pendingAuditWrites` from `harness.runtime.outstandingDeferred()` filtered to the `'audit-write'` label, and `openStorageResources` from the memory storage fixture's own public call accounting (`BureauStorageFixture.openHandles()` — sqlite/lmdb fixtures never open their own handle, so theirs always reads `[]`). The first three are read AFTER `bureau.shutdown()` returns but are deliberately never folded into `leaked`: the owner each names (`scheduler`/`online-evals`/`audit-trail`) is already reported under `incomplete` when a bounded `shutdown()` times out on it, and promoting the same fact into `leaked` too would double-report it. `openStorageResources` IS folded into `leaked` — nothing in `bureau.shutdown()`'s own owner accounting shadows a lingering low-level storage call, so this is the only place that leak is ever reported. `incomplete` carries any owner `bureau.shutdown({ timeoutMilliseconds })` itself classified `'unresolved'` — reported distinctly from `leaked`, never promoted into it.

```typescript
harness.registerDurableRun(summary.id, { detached: true });
const report = await harness.close();
report.detached; // [{ kind: 'durable-owner', id: summary.id }] — not a leak
```

A durable run registered with `detached: true` is recorded under `detached`, never under a leak row — a deliberate, recorded outcome (AB-34), not a way to hide a leak.

### `assembleReproductionArtifact(harness, recorder, options)` (AB-263)

Builds on a `harness` already constructed with `createBureauTestHarness` (see above):

```typescript
import { createEventRecorder } from '@lostgradient/operative/test';
import { assembleReproductionArtifact } from 'bureau/test';

const recorder = createEventRecorder(harness.runtime);
const run = harness.startRun('worker', 'hello');
recorder.attachIterable(run, { kind: 'run', id: 'worker-run' });

const terminalResult = await run.result();
const cleanupReport = await run.closed();
await harness.runtime.deferred.drain();

const artifact = await assembleReproductionArtifact(harness, recorder, {
  terminalResult,
  cleanupReport,
});
```

`ReproductionArtifact` here is `@lostgradient/operative/test`'s canonical declaration (AB-334), instantiated with Bureau's own `cleanupReport` union — `CleanupAcknowledgement | DeferredDrainReport | BureauShutdownReport` — since `bureau` can import that shared shape from `operative` but never the reverse. Assembles the normalized `ReproductionArtifact` AB-92's decision record fixes (AC8) from one harness run: `sourceRevision` (`git rev-parse HEAD`, read once), `packageVersions` (every workspace package's `name`/`version`, read from its `package.json`), `effectiveModel` (`harness.bureau.getConfiguration().provider` — requires `BureauOptions.provider` to be set alongside a scripted `generate`; throws rather than inventing a provider/model when it is not), `clockOrigin`/`identifierSeed`/`randomSeed` (`harness.runtime`'s own recorded seeds — present even for an unpinned, default-constructed runtime), `scriptedOutcomes`/`firedFaults` (always empty until AB-95's fault engine exists), `causalTrace` (`recorder.normalize()` and nothing else), `terminalResult`, and `cleanupReport`. `terminalResult` is redacted through `summarizeToolInput` before embedding — a privileged key (`password`, `secret`, `token`, `apiKey`, `authorization`, `credential`, `privateKey`) never appears in the serialized artifact. The returned object's keys are always written in AB-92's fixed field order, so `JSON.stringify` is byte-identical across two runs of the same scripted case with the same seeds.

`assembleReproductionArtifact` takes an optional fourth `environment` argument (AB-264): `{ sourceRevision?, packageVersions? }`. Supplying a field there replaces this function's own discovery for that field — `git rev-parse HEAD` and the `packages/*` manifest glob both assume a real repository checkout rooted at a `turbo.json`, neither of which exists inside a packed-and-path-installed tarball consumer (`scripts/verify-bureau-tarball-boundary.ts`'s probe passes both explicitly, since it already knows them from its own pack step). Omitted fields still discover normally, so every existing caller inside this repository is unaffected.

```typescript
const artifact = await assembleReproductionArtifact(
  harness,
  recorder,
  { terminalResult, cleanupReport },
  { sourceRevision, packageVersions },
);
```

### Bureau-scoped fault-plan vocabulary and selectors (AB-263)

```typescript
import {
  selectAuditWriteFaultTarget,
  selectSchedulerTaskFaultTarget,
  selectWebhookDeliveryFaultTarget,
  type BureauFaultPlan,
} from 'bureau/test';
```

`BureauFaultOperation` widens `@lostgradient/operative/test`'s `FaultOperation` with three Bureau-owned boundaries operative cannot see: `'webhook-delivery'`, `'audit-write'`, and `'scheduler-task'`. `BureauFaultPlanEntry`/`BureauFaultPlan` mirror operative's `FaultPlanEntry`/`FaultPlan` with that widened operation; every other fault type (`FaultBoundary`, `FaultOccurrence`, `FiredFault`, ...) is re-exported from operative verbatim — there is exactly one `FaultBoundary` union in the repository. No engine executes a `BureauFaultPlan` yet (AB-95); what ships here is the type vocabulary plus three selector functions that resolve a Bureau-scoped target to exactly one concrete resource, throwing `BureauFaultSelectorResolutionError` otherwise: `selectWebhookDeliveryFaultTarget(deliveries, deliveryId)` (by `WebhookDeliveryRecord.id`, from `WebhookNotifier.listDeliveries()`), `selectAuditWriteFaultTarget(records, auditEntryKey)` (by the audit entry key `encodeAuditEntryKey` — also exported from this package's root — produces from a queried `AuditRecord`'s `timestampMs`/`sequence`/`runId`), and `selectSchedulerTaskFaultTarget(tasks, taskId)` (by `SubmitSchedulerTaskResponse.taskId`).

### Storage fixtures

```typescript
import {
  createLmdbStorageFixture,
  createMemoryStorageFixture,
  createSqliteStorageFixture,
} from 'bureau/test';
```

Each factory returns a `BureauStorageFixture`: `{ configuration, path?, owned, dispose(), openHandles() }`. `configuration` passes straight to `BureauOptions.storage`. `createSqliteStorageFixture`/`createLmdbStorageFixture` take `{ runtime, path? }` — omit `path` for a fresh, unique path allocated from `runtime.identifiers.next('storage-fixture')` (`owned: true`, removed by `dispose()`), or supply your own `path` for a fixture that never allocates or deletes anything (`owned: false`). Their fixture itself never opens a live storage handle — the real backend connection is owned and closed by whatever composes `configuration` (typically `Bureau`), so `dispose()` only ever governs the filesystem lifecycle of a path this fixture allocated, and `openHandles()` always returns `[]` (nothing of their own to account for).

`createMemoryStorageFixture(options?: { wrapStorage?: (storage: Storage) => Storage })` (AB-322) is the one exception: it constructs its own `MemoryStorage` instance directly — safe only for memory, since there is no real file handle or single-writer lock to race the way sqlite/lmdb would — and hands `Bureau` that INSTANCE as `configuration`, wrapped with public call accounting. `openHandles(): readonly string[]` names every call (`get`/`put`/`delete`/`scan`/`batch`/…) this fixture has seen START but not yet FINISH — empty on the happy path, and a real diagnostic once something (e.g. `createFaultEngine(...).wrapStorage`, passed through `options.wrapStorage` before this fixture's own accounting wraps the result) blocks a call deterministically. `dispose()` disposes that instance directly — `bureau.shutdown()` never closes a caller-supplied `Storage` instance, only one it resolved itself from a `StorageConfiguration`, so this fixture is the one thing that does.

## Development

Run package checks from this directory:

```bash
bun run validate
bun run build
```
