{
  "$schema": "https://turborepo.dev/schema.json",
  "ui": "tui",
  "globalDependencies": [
    ".prettierrc.json",
    "bunfig.toml",
    "tsconfig.base.json",
    "eslint.config.base.ts",
    "scripts/determinism-manifest.json"
  ],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"],
      "outputLogs": "new-only"
    },
    "check-types": {
      "dependsOn": ["^build"],
      "outputs": []
    },
    "@lostgradient/operative#check-types": {
      "dependsOn": ["^build", "build"],
      "outputs": []
    },
    "integration": {
      "dependsOn": ["^build"],
      "outputs": [],
      "passThroughEnv": ["REPRODUCTION_ARTIFACT_DIR"]
    },
    "test": {
      "dependsOn": ["^build", "build"],
      "outputs": [],
      "passThroughEnv": ["CI", "NODE_BINARY", "NODE_ENV", "PATH"]
    },
    "coverage:check": {
      "dependsOn": ["^build", "build"],
      "outputs": ["coverage/**"]
    },
    "format:check": {
      "outputs": []
    },
    "lint": {
      "dependsOn": ["^build"],
      "outputs": []
    },
    "dev": {
      "cache": false,
      "persistent": true
    }
  }
}
